# Whole-Codebase Weakness Audit — 2026-07-15

Adversarial multi-agent audit (20 subsystem/lens finders, refute-by-default 2-verifier verification). **63 raised → 47 confirmed.** Severity: 8 high, 20 medium, 19 low; 20 touch the decision-trio/invariant.

`INV` = fix touches the CI-gated trio (ev_engine/wheel_runner/candidate_dossier) or an EV/downgrade path → **operator review required, not auto-dispatched.**

---

## [HIGH] data/consolidated_loader.py:235
**consolidated_loader returns rotated OHLCV uncompensated: get_ohlcv/get_spot_price yield OPEN price mislabeled as 'close'**

- **Category:** schema-rotation  |  **Area:** connector-data
- **Failure scenario:** The on-disk sp500_ohlcv.csv ships column labels rotated one position (physical open=HIGH, high=CLOSE, close=OPEN, low=LOW; confirmed on row 1 of A UN Equity 2018-01-02 where labeled open=67.87 > labeled high=67.6, impossible for a real bar). MarketDataConnector.get_ohlcv (data_connector.py:518-524) applies the compensating rename {open->high, high->close, close->open}. ConsolidatedBloombergLoader.load_ohlcv only lowercases columns (line 235) and applies NO rename, yet its own module docstring calls it 'the primary loader for the Smart Wheel Engine'. As a result get_ohlcv(...)['close'] and get_spot_price(...) (line 608, `ohlcv['close'].iloc[-1]`) return the physical close column, which actually holds the OPEN price; 'high' holds the true CLOSE and 'open' holds the true HIGH. Any consumer of this loader (data/pipeline.py -> data/feature_pipeline.py get_spot_price at 643 for assignment/vol/technical features; backtest frame builder at pipeline.py:628 `close=df['Close']`) computes spot, returns, moneyness, and assignment risk off the wrong bar — systematically ~the open-close gap off, and high/open fully swapped. Silent wrong features feeding any downstream ranking or risk number.
- **Fix sketch:** Apply the same rename in load_ohlcv immediately after lowercasing (rename open->high, high->close, close->open) and add the same high>=max(o,c,l) sample-invariant guard data_connector uses, so both loaders share one price contract. Better: have consolidated_loader import/share the single rename+validator so drift can't reintroduce divergence.
- **Verifier:** Bug is real and reachable. Confirmed the on-disk data/bloomberg/sp500_ohlcv.csv ships rotated columns: row 1 (A UN Equity 2018-01-02) has labeled open=67.87 > labeled high=67.6, impossible for a real bar; row 2 repeats it — systematic. MarketDataConnector.get_ohlcv (engine/data_connector.py:518-524) applies the compensating rename {open->high, high->close, close->open} plus a documented sample-inv

## [HIGH] engine/data_connector.py:1598
**PIT dividend-yield lookup silently misses BRK.B/BF.B (slash vs dot), leaking current carry-q into backtests on the EV path**

- **Category:** pit-lookahead  |  **Area:** connector-data
- **Failure scenario:** In `_pit_dividend_yield`, `key = normalize_ticker(ticker)` uses data_connector's normalize which KEEPS the slash ('BRK/B' -> 'BRK/B', 'BF/B' -> 'BF/B'), but the dividend_pit panel's `ticker_normalized` column is built by BroadPullLoader via consolidated_loader.normalize_ticker which maps slash->dot ('BF/B UN' -> 'BF.B'). The comparison `panel['ticker_normalized'] == key` ('BF.B' == 'BF/B') is therefore always False for the two slash names, so `_pit_dividend_yield` returns None. get_fundamentals(ticker, as_of=...) then falls back to the CURRENT (2026) snapshot `eqy_dvd_yld_12m`. The puts ranker (wheel_runner.py:1666) threads as_of into get_fundamentals since #428, so a dated backtest on BRK.B or BF.B (both S&P 500 members) at e.g. as_of=2015 prices the BSM carry-q with the 2026 dividend yield instead of the point-in-time value present in sp500_dividend_yield_pit.csv — a genuine carry-q lookahead that moves EV/ranking. Confirmed: sibling `_snapshot_bdp_next_earnings` (line 941-943) applies `.replace('/', '.')` to BOTH sides to close exactly this mismatch; `_pit_dividend_yield` was left unpatched and the code comment at line 938-940 explicitly acknowledges it.
- **Fix sketch:** Normalize both sides to a single share-class form before comparing, mirroring `_snapshot_bdp_next_earnings`: `key = normalize_ticker(ticker).replace('/', '.')` and compare against `panel['ticker_normalized'].astype(str).str.replace('/', '.')` (or `.str.replace('/', '.', regex=False)`).
- **Verifier:** Confirmed against the code. engine/data_connector.py:55 defines a slash-KEEPING normalize_ticker ("BRK/B UN Equity" -> "BRK/B"), while data/consolidated_loader.py:75 defines a slash->dot normalize (`ticker.replace("/", ".")`, "BRK/B UN Equity" -> "BRK.B"). The dividend_pit panel's `ticker_normalized` column is built by BroadPullLoader (data/broad_pull_loaders.py:311) using the dot-form data-side n

## [HIGH] `INV` engine/ev_engine.py:350
**EVEngine.slippage_pct_of_spread is a dead parameter — configured slippage never reaches the fill model**

- **Category:** silent-wrong-config  |  **Area:** costs
- **Failure scenario:** An operator constructs EVEngine(slippage_pct_of_spread=0.50) to model conservative worse-than-mid taker fills. The value is stored on self.slippage_pct_of_spread (line 278) but evaluate() calls calculate_slippage(mid_price=trade.premium, bid_ask_spread=spread, trade_direction='sell', open_interest=...) at line 350-355 WITHOUT passing it, so slippage silently uses the module constant DEFAULT_SLIPPAGE_PCT=0.15. Grep confirms slippage_pct_of_spread is only assigned, never read anywhere. Worse, the documented constructor default (0.20) already disagrees with the value actually used (0.15), so even the out-of-the-box EV is computed at a 25% smaller slippage fraction than the class advertises. Result: every ev_dollars is optimistic vs the operator's intended cost model, and any tuning of this knob has zero effect — a silent no-op on the CI-gated decision path.
- **Fix sketch:** Thread self.slippage_pct_of_spread into calculate_slippage via a new base_factor override param (default None → DEFAULT_SLIPPAGE_PCT), or delete the dead constructor param and reconcile the docstring. Reconcile the 0.20-vs-0.15 default mismatch either way. Re-baseline the EV-authority backtests since ev_dollars will shift.
- **Verifier:** Confirmed dead parameter. engine/ev_engine.py:258 declares constructor default slippage_pct_of_spread=0.20; line 278 stores it on self.slippage_pct_of_spread. Repo-wide grep returns ONLY those two lines — the attribute is assigned and never read. evaluate() at ev_engine.py:350-355 calls calculate_slippage(mid_price, bid_ask_spread, trade_direction, open_interest) with no way to inject the configur

## [HIGH] `INV` engine/portfolio_risk_gates.py:978
**C4 stress silently substitutes strike for missing spot, corrupting the R8 downgrade decision**

- **Category:** silent-wrong  |  **Area:** stress
- **Failure scenario:** check_stress_scenario builds full_spots via full_spots.setdefault(p['symbol'], p['strike']). Consider a held short put strike=100 whose real spot=130 (deep OTM, tiny loss under -10%/+30% IV). If spot_prices lacks that symbol, full_spots['SYM']=100 (=strike), so StressTester.run_scenario prices it ATM. Under the C4 -10% spot / +30% IV shock the ATM put loses far more, inflating drawdown_pct past the 8% NAV limit and firing R8 (downgrade proceed->review) on a trade that is actually safe. The mirror case (real spot below strike / ITM) understates the loss and fails to fire R8 when it should. The module explicitly has a 'missing_data' pass convention (D11 'no silent substitution'), which this strike fallback bypasses.
- **Fix sketch:** Do not substitute strike for a missing spot. If a position's symbol is absent from spot_prices, return GateResult(passed=True, reason='missing_data', details={...}) instead of setdefault(symbol, strike), matching the module's missing_data semantics.
- **Verifier:** Confirmed real and reachable. engine/portfolio_risk_gates.py:976-978 builds full_spots and does full_spots.setdefault(p['symbol'], p['strike']) for every position — a deliberate strike-as-spot fallback (the sibling check_portfolio_delta at line 687-690 carries the comment 'fall back to strike if missing'). The upstream filter _filter_bsm_safe_positions (line 201-234) only drops rows with a missing

## [HIGH] `INV` engine/regime_hmm.py:363
**HMM 'bull_quiet' label is assigned by within-window rank, not absolute return sign → 1.25x EV up-sizing on calm days of a bear grind**

- **Category:** regime-label-leakage  |  **Area:** regime-dealer
- **Failure scenario:** _label_states(K=4) unconditionally returns ["crisis","bear","normal","bull_quiet"] purely by the ascending sort of scores = means[:,0] - 0.5*stds[:,0] (regime_hmm.py:225,363-364). position_multiplier (regime_hmm.py:302-312) then weights the top-ranked state 'bull_quiet' at 1.25 regardless of whether that state's mean return is actually positive. Concrete trigger: a name in a steady low-vol decline (~ -0.1%/day drift, ~1% daily vol over the 504d fit window, i.e. ~ -25% annualized) fits 4 states ALL with negative means; state 3 (least-negative drift, lowest vol) is labeled 'bull_quiet'. On a calm small-move decision day the filtered posterior probs[-1] concentrates on state 3, so hmm.position_multiplier -> ~1.25 (wheel_runner.py:2003). That flows into combined_regime_mult (wheel_runner.py:2154) -> trade.regime_multiplier -> EVEngine scales ev_dollars = ev_raw * regime_mult ~ +25% (ev_engine.py:556,594). The engine systematically up-sizes/ up-ranks short-put EV on falling knives exactly on the quiet days a seller feels safe — the assignment trap. The S33 comment (wheel_runner.py:1964-1972) only surfaces realized vol/return as a diagnostic; it does NOT correct the multiplier, and it flags the 'crisis-with-positive-return' case, not this opposite 'bull_quiet-with-negative-return' up-sizing.
- **Fix sketch:** Make the label reflect the absolute state mean, not just rank: in _label_states gate 'bull_quiet'/'normal' on means[k,0] > 0 (and 'crisis'/'bear' on means[k,0] < 0), OR in position_multiplier clamp the weight to <=1.0 when the argmax/weighted state's absolute annualized mean is <= 0. Simplest robust fix: cap position_multiplier at 1.0 whenever hmm_realized_return_252d_ann (already computed at wheel_runner.py:1987) is negative.
- **Verifier:** Confirmed by reading the code. regime_hmm.py:363-364 `_label_states` for K==4 unconditionally returns ["crisis","bear","normal","bull_quiet"] by sort rank (scores=means[:,0]-0.5*stds[:,0], line 225-226), with no reference to the absolute sign of each state's mean. position_multiplier (302-312) maps the label 'bull_quiet' to weight 1.25 via pure label lookup, no sign gate. The path is LIVE and reac

## [HIGH] `INV` engine/risk_manager.py:651
**Dollar-gamma P&L is 100x understated in VaR and stress (gamma_dollars carries /100 but is paired with fractional moves)**

- **Category:** greek-unit-error  |  **Area:** pricer-greeks
- **Failure scenario:** calculate_portfolio_greeks builds delta_dollars = delta*shares*spot (line 362, NO /100) but gamma_dollars = gamma*shares*spot^2/100 (line 363, WITH /100). Every gamma-P&L consumer then multiplies gamma_dollars by a *fractional* move squared: historical VaR gamma_pnl = 0.5*gamma_dollars*hist_return^2 (line 651, comment 'since gamma_dollars already scaled'), parametric VaR (line 542), and run_stress_tests (lines 1259/1303/1349/1376). Because the delta term correctly pairs delta_dollars with the fractional return while the gamma term pairs gamma_dollars(/100) with the same fractional return squared, the gamma/convexity contribution is exactly 100x too small (verified: a 10-lot short $95 put at S=100,T=0.10,sigma=0.30 has true dollar-gamma P&L of $1712 for a -10% move but the code reports $17.12). Concrete trigger: R7 (candidate_dossier VaR soft-warn) calls portfolio_risk_gates.check_var -> rm.calculate_var with returns_data, hitting the historical path at line 651. A concentrated short-put/short-gamma wheel book whose true 30-day 95% VaR exceeds 5%xNAV largely because of convex gamma losses reports a VaR missing ~all of its gamma term, so it lands under the 5% threshold and R7 fails to downgrade proceed->review. The same 100x error makes every dashboard crash/gap stress number (run_stress_tests) far too rosy for short options.
- **Fix sketch:** Make the gamma dollar convention consistent with the (correct) delta convention: drop the /100 at line 363 so gamma_dollars = gamma*shares*spot^2, and keep gamma_pnl = 0.5*gamma_dollars*move_fraction^2 everywhere. Then update GREEKS_UNIT_CONTRACT.md (which currently defines gamma_dollars = gamma*spot^2*contracts*mult/100 paired with (spot_change_pct)^2 -- only self-consistent if spot_change_pct is in percent, which no caller uses) and re-check the max_portfolio_gamma_dollars limit (line 1479) and dashboard display against the new scale. Add a P&L-decomposition regression (delta+gamma+vega vs full reprice within 1%) for a short-gamma book, per the contract's own validation section.
- **Verifier:** Confirmed genuine unit inconsistency and confirmed reachable via the normal (non-edge) path. At engine/risk_manager.py:362 delta_dollars = delta*multiplier*spot (no /100); at :363 gamma_dollars = gamma*multiplier*spot*spot/100 (WITH /100). In _historical_var, hist_returns are fractional (line 630 uses .pct_change()); delta_pnl = delta_dollars*return (line 645) is dimensionally correct (delta_total

## [HIGH] `INV` engine/wheel_runner.py:1672
**Backtest lookahead: IV fallback silently substitutes today's snapshot IV when PIT IV history is unavailable at as_of**

- **Category:** lookahead-leak  |  **Area:** wheel-runner
- **Failure scenario:** In a backtest, `_resolve_pit_atm_iv(conn, ticker, as_of)` returns None whenever the ticker's `vol_iv` history has no row on/before `as_of` (e.g. as_of predates IV-table coverage while OHLCV — required only 504d — still covers it) or is >30d stale (the W36 gate). All three rankers then fall through to `iv_raw = fundamentals.get('implied_vol_atm')` (lines 1672 put / 3085 covered-call / 3718 strangle). `MarketDataConnector.get_fundamentals` (data_connector.py:1565) returns `implied_vol_atm` from the CURRENT snapshot column `30day_impvol_100.0%mny_df` regardless of as_of — only `dividend_yield` is made PIT. So a candidate dated e.g. 2020-03-01 is priced with 2026 IV. IV is a first-order input to the brentq delta->strike solve, the synthetic BSM premium, and `fair_value`/`edge_vs_fair`, so `ev_dollars`, `ev_per_day`, `roc` and the entire ranking order are contaminated with hindsight. It is fully silent: `distribution_source` stays 'empirical', `premium_source` stays 'synthetic_bsm', nothing flags the IV as a future snapshot, and the candidate is RANKED (not dropped). The S23 F3 comment (lines 1634-1646) documents that snapshot IV mis-prices historical as_of (AVGO 42.96% snapshot vs 49.5% PIT = 15% relative error) — the exact leak the fallback reintroduces. Note the asymmetry with the spot-staleness gate, which REFUSES rather than silently substituting.
- **Fix sketch:** When `as_of is not None` (backtest) and `_resolve_pit_atm_iv` returns None, DROP the candidate with a `data` gate reason ('no PIT IV available at as_of') instead of falling back to the snapshot `implied_vol_atm` — mirroring the spot-staleness gate's refuse-don't-substitute (D11) philosophy. Keep the snapshot fallback only for `as_of is None` (live, where the snapshot IS current) and for connectors lacking `get_iv_history`. Apply identically at 1672, 3085, 3718.
- **Verifier:** Confirmed against code. wheel_runner.py:1668-1672 (puts), 3083-3085 (covered call), 3716-3718 (strangle): when _resolve_pit_atm_iv returns None, all three rankers unconditionally fall through to iv_raw = fundamentals.get('implied_vol_atm') with no `as_of is not None` guard. data_connector.py:1565 proves that value comes from the CURRENT snapshot column 30day_impvol_100.0%mny_df; the as_of branch (

## [HIGH] `INV` engine/wheel_tracker.py:1066
**roll_put / roll_call open a new short-option leg bypassing BOTH the EV-authority token gate and the D17 concentration caps that open_short_put/open_covered_call enforce**

- **Category:** invariant-bypass  |  **Area:** tracker
- **Failure scenario:** A roll closes the old option and opens a genuinely NEW short option at a new strike/premium/expiration, but roll_put (line 1066) and roll_call (line 1164) reference none of the launch gates: no `if self.require_ev_authority` consume (unlike open_short_put L702 / open_covered_call L1420), and no `_evaluate_d17_hard_blocks` call (unlike L751 / L1454). Concrete #1 (production default): a tracker from make_live_book_tracker() has enforce_sector_cap=True, enforce_single_name_cap=True, so open_short_put HARD-refuses a single-name notional over 10% NAV (check_single_name_cap). Open AAPL 150P sized just under the 10% cap, then call `roll_put('AAPL', roll_date, new_strike=300, new_premium=..., new_expiration=..., new_iv=..., buyback_price=...)`; the position's single-name collateral roughly doubles to ~20% NAV and the roll is applied with ZERO cap check — the very concentration invariant test_production_tracker_caps.py pins is silently escapable. Concrete #2 (strict mode, require_ev_authority=True): the rolled put is a new strike/expiry that never went through EVEngine.evaluate and needs no token, directly contradicting CLAUDE.md §2 ('no tradeable candidate bypasses EVEngine.evaluate') and the constructor's claim that strict mode 'prevents heuristic / manual / webhook paths from entering a position bypassing the EV engine'.
- **Fix sketch:** Route the roll's open-leg through the same gate as opens: in strict mode require+consume a fresh ev_authority_token/current_ev_dollars for the NEW leg, and always call _evaluate_d17_hard_blocks (sector/single-name) on the new strike*100 notional before mutating pos. Alternatively, forbid raw roll_put/roll_call when require_ev_authority or enforce_*_cap is set and force the roll through an EV-routed helper (mirroring consume_ranker_row) that reuses suggest_rolls output.
- **Verifier:** Confirmed by reading engine/wheel_tracker.py roll_put (L1066-1162) and roll_call (L1164-1249): both close the old option and open a genuinely new short leg (new strike/premium/expiration at L1132-1137 and L1230-1235), yet neither body contains a require_ev_authority token consume nor a _evaluate_d17_hard_blocks call. The only guards are ticker-present and state (SHORT_PUT/COVERED_CALL) checks — bo

## [MEDIUM] backtests/regression/_common.py:673
**"none" friction level is not frictionless and "full" double-charges commissions: WheelTracker applies its own unconditional cost model underneath the harness overlay**

- **Category:** friction  |  **Area:** backtests
- **Failure scenario:** Every harness driver builds the tracker with plain WheelTracker(initial_capital, connector) (_common.py:511, 981) which has NO cost-disable flag. open_short_put unconditionally runs calculate_total_entry_cost (wheel_tracker.py:765) -> deducts $0.65/contract commission + ~1.5% premium slippage on every open; handle_put/call_assignment unconditionally deduct a $5.00 DEFAULT_ASSIGNMENT_FEE (wheel_tracker.py:962/995). The harness then layers friction ON TOP: it passes friction_adjusted_premium into open_short_put AND, at 'full', separately subtracts friction_open_cost($0.65) and friction_assignment_cost(10bp+$0.65) from tracker.cash (_common.py:283, 545). Net effect: (a) the S27 headline snapshot is labeled friction_level='none'/frictionless (docstring _common.py:27) yet its locked final_nav ($151,444) silently bakes in ~$0.65+1.5% per open and $5 per assignment; (b) at 'full' the open commission is charged twice ($0.65 tracker + $0.65 harness) and assignment pays $5 tracker + (10bp+$0.65) harness. Concrete: 24-ticker $100k run, ~200 opens/yr -> the 'frictionless' baseline is understated by thousands of dollars vs a true zero-cost run, and the S32/S34 friction-DECOMPOSITION (the entire point of those snapshots) mis-attributes drag because its baseline is contaminated and its top level double-counts.
- **Fix sketch:** Either (1) run the tracker in a genuinely cost-free mode for the harness and let the friction overlay be the SOLE cost model (add a costs-off path to WheelTracker / calculate_total_entry_cost and pass raw premium at 'none'), or (2) drop the harness overlay's premium adjustment + explicit cash deductions and drive all friction through the tracker's own parameters. Do not stack both. Re-baseline S27/S32/S34/S35 after fixing and relabel 'none' honestly.
- **Verifier:** Verified by reading code. Both harness drivers construct WheelTracker(initial_capital, connector) with no cost-disable path (_common.py:511 single driver; :981-982 multi-friction dict). open_short_put unconditionally calls calculate_total_entry_cost (wheel_tracker.py:765); that function (engine/transaction_costs.py:231-254) always returns commission=0.65 (calculate_commission "option") and a slipp

## [MEDIUM] data/consolidated_loader.py:576
**consolidated_loader.get_risk_free_rate keeps the value-based percent/decimal heuristic, giving 100x-wrong rates in the ZIRP era**

- **Category:** unit-conversion  |  **Area:** connector-data
- **Failure scenario:** get_risk_free_rate returns `rate/100 if rate > 1 else rate`. The treasury CSV is authoritatively in PERCENT (rate_3m of 0.04 means 0.04%). For any sub-1% percent reading (~56% of rate_3m rows across 2011-2022 per the D20 note), the `rate > 1` branch is not taken, so 0.04% is returned as 0.04 = 4% decimal — a 100x overstatement of the risk-free rate. This is the exact bug MarketDataConnector.get_risk_free_rate was fixed for under D20 (divide by 100 unconditionally), but consolidated_loader still carries the old heuristic. It feeds data/pipeline.py:247 `self._risk_free_rate` and any BSM/pricing that consumes the consolidated/pipeline loader, mispricing options and carry throughout the low-rate era. Also returns a silent default 0.05 (line 569) when treasury is missing, masking absent data instead of surfacing NaN.
- **Fix sketch:** Divide by 100 unconditionally (matching data_connector D20) and return NaN rather than a 0.05 default when treasury data is unavailable, so callers detect missing data instead of silently using 5%.
- **Verifier:** The code defect is confirmed real, not dead code. consolidated_loader.py:576 carries `return rate / 100 if rate > 1 else rate` and line 569 returns a silent 0.05 default, and this method executes at pipeline.py:247 during load. DECISIONS.md D20 (lines 1192-1221) explicitly declares this value-based heuristic a 100x error for sub-1% percent ZIRP-era rates and mandates unconditional /100; its 'Pinne

## [MEDIUM] `INV` engine/candidate_dossier.py:610
**R7-R10 portfolio soft-warns are contract-count-blind: multi-contract candidate risk understated by the contract multiplier, so an oversized single-name position stays "proceed" instead of "review"**

- **Category:** risk-underestimate  |  **Area:** dossier-reviewers
- **Failure scenario:** Operator runs WheelRunner().rank_and_review(tickers=[...], contracts=5, portfolio_context=pc) on a $500k NAV book. For candidate XYZ (short put, strike $200) the EV path (wheel_runner.py:2166) scales ev_dollars to 5 contracts, so ev_dollars is large-positive and R5 sets verdict=proceed. The TRUE marginal notional is 200*100*5 = $100k = 20% of NAV on one name, which should breach the F4 single-name 10% cap (R10) and downgrade proceed->review. But the put-side ranker row (rank_candidates_by_ev, wheel_runner.py:2344-2534) never emits a 'contracts' key (unlike the covered-call/strangle rows), so R10's `contracts = int(ev_row.get('contracts', 1))` (candidate_dossier.py:524) yields 1 and computes proposed_notional = 200*100*1 = $20k = 4% NAV -> passes the 10% cap. R9 sector cap (line 475/479) under-counts by 5x identically, and R7 VaR / R8 stress call _build_candidate_dict which hardcodes 'contracts': 1 (line 610), so the candidate leg is priced as a single contract inside check_var/check_stress. Net: all four portfolio soft-warns evaluate the candidate at 1/5 of its real size, and the exact case R10/R11 exist to catch (large idiosyncratic single-name bet) silently emits 'proceed' rather than the intended 'review'.
- **Fix sketch:** Make contract count a single source of truth: emit 'contracts' on the put-side ranker row (wheel_runner.py rank_candidates_by_ev) as the covered-call/strangle rows already do, and change _build_candidate_dict (candidate_dossier.py:604-612) to read contracts = int(ev_row.get('contracts', 1)) instead of hardcoding 1, so R7/R8 (VaR/stress) and R9/R10 (sector/single-name) all see the real size. Add a regression asserting a contracts>1 candidate that breaches the single-name cap downgrades to review.
- **Verifier:** Confirmed by reading the code. The put-side ranker row in rank_candidates_by_ev (engine/wheel_runner.py:2344-2532, core + diagnostic blocks) never emits a "contracts" key, unlike the covered-call row (line 3366) and strangle row (line 4083) which both emit "contracts": contracts. Meanwhile ev_dollars IS scaled by contracts (ShortOptionTrade contracts=contracts at line 2166; collateral = strike*100

## [MEDIUM] `INV` engine/dealer_positioning.py:623
**Gamma-flip level solved from BSM gammas while gex_total/regime uses stored gammas → flip_level is not the zero of the regime it labels**

- **Category:** gex-flip-inconsistency  |  **Area:** regime-dealer
- **Failure scenario:** ms.gex_total (which sets the regime sign via _classify_regime, dealer_positioning.py:391,701-705) is the sum of per-strike net_gex from _greek_row, which PREFERS the chain's stored gamma when finite (dealer_positioning.py:532-536). But _solve_flip_level recomputes gamma exclusively from black_scholes_all_greeks and ignores stored gamma (dealer_positioning.py:623-636). When a provider (e.g. Theta/Bloomberg on the laptop path where chain_df is non-None) supplies stored gammas that differ from the engine's BSM reconstruction (different r/q, American-style, or a different vol footing), total_gex(spot) in the flip scan can have a DIFFERENT sign/magnitude than ms.gex_total. Result: flip_level is not the zero of the quantity the regime is actually classified on. Concrete: stored-gamma gex_total is strongly negative (regime='short_gamma_amplifying', dealer_mult drives toward 0.70 and R6 can downgrade), yet the BSM flip scan finds a zero-crossing right at spot -> flip_distance_pct ~0 -> _classify_regime OVERRIDES to 'near_flip' (dealer_positioning.py:698-699) -> dealer_mult becomes 0.85 and R6 fires 'dealer_near_flip' review instead of the short-gamma path. The label, the ev_dollars multiplier, and the R6 downgrade all key off two inconsistent gamma sources.
- **Fix sketch:** Make the flip scan use the same gamma source as gex_total: either have _solve_flip_level reuse stored gammas (scale per-strike stored gamma to spot s), or make _greek_row/gex_total always recompute from BSM so both agree. At minimum, when stored gammas are present, compute flip_level from the same per-strike gammas rather than a second independent BSM pass.
- **Verifier:** CONFIRMED as a real, reachable inconsistency on the default Bloomberg laptop path. The code uses two different gamma sources for two quantities that are supposed to describe the same curve: gex_total (dealer_positioning.py:368) sums per-strike net_gex whose gamma comes from _greek_row, which prefers the chain's stored gamma when finite (lines 503-504, 532-536); _solve_flip_level (line 597, gamma a

## [MEDIUM] `INV` engine/ev_engine.py:495
**GPD tail (cvar_99_evt/tail_xi/heavy_tail) is skipped on the default empirical tier and, when it runs, is fit to autocorrelated overlapping returns**

- **Category:** correctness-iid-violation  |  **Area:** tail-copula
- **Failure scenario:** The EVT block is gated `if len(pnls) >= 200` (ev_engine.py:495). The default forward-distribution tier for deep-history S&P names is empirical_non_overlapping with N ~ 30-35 (forward_distribution.py:419 and its docstring), which is always < 200, so cvar_99_evt and tail_xi are always NaN and heavy_tail always False for the entire long-history population -- the 'more reliable 1% CVaR than historical-simulation' claimed at ev_engine.py:487-491 is inert for them. It only runs for short-history names that fall to the empirical_overlapping tier (forward_distribution.py:358-367), which returns OVERLAPPING N-day log-returns (each shares horizon_days-1 days with its neighbor). fit_gpd_tail then fits a GPD to heavily autocorrelated exceedances, violating the iid assumption behind the MLE: on a 2-yr history (~469 overlapping 35-day returns) the ~24 nominal exceedances represent only ~1-2 independent drawdown episodes. Measured over 59 such histories, xi mean -1.18, std 0.45, frequently pinned at scipy's -2 boundary. So tail_xi is a meaningless diagnostic on that tier and cvar_99_evt inherits the clustering bias. Net: the EVT tail is dead for long-history names and unreliable for short-history names -- it never delivers the promised reliability, and the fields are surfaced to the operator (wheel_runner.py:2419-2423) as if trustworthy.
- **Fix sketch:** Require an iid scenario source before fitting the GPD: only compute the EVT tail when distribution_source is block_bootstrap / har_rv / lognormal (independent paths), never on empirical_overlapping. Reuse forward_distribution.is_iid_forward_source-style gating. Alternatively down-weight the effective exceedance count by the overlap factor and widen the min_exceedances requirement so autocorrelated windows can't converge to a spurious xi.
- **Verifier:** Every factual claim checks out against the code. (1) The EVT block at ev_engine.py:495 is gated solely by `if len(pnls) >= 200:` with no iid/source check. (2) `len(pnls)` equals the forward-distribution scenario count: `_build_terminal_prices` (ev_engine.py:665-670) returns the passed array unchanged and the wheel_runner widening step (2287) preserves count. (3) The default tier `empirical_non_ove

## [MEDIUM] `INV` engine/ev_engine.py:524
**ev_dollars is not net of exit costs — exit commission + slippage are computed but never subtracted (docstring claims otherwise)**

- **Category:** exit-cost-omission  |  **Area:** costs
- **Failure scenario:** exit_commission and exit_slippage are computed (lines 358-362) and folded into total_transaction_cost (reported field), but the D19 note at 524-532 confirms they are NOT subtracted from ev_raw/ev_dollars — only entry costs enter net_premium_in (line 365), and _compute_pnls uses net_premium_in. The class docstring (lines 50-51) asserts 'Commissions and slippage are applied to both entry AND exit legs,' which is false for the EV number. Concretely, for premium $2.00, spread $0.10, 1 contract: omitted exit cost ≈ exit_slippage $1.50 + exit_commission $0.65 ≈ $2.15/contract. On a candidate whose true EV is ~$25/contract this overstates ev_dollars by ~9%, and because exit_slippage=entry_slippage scales with spread, the overstatement again biases the ranking toward wide-spread names (compounding finding 2). Two candidates with identical true net EV but different spreads rank in the wrong order.
- **Fix sketch:** Ship the already-authored D19 fix: subtract (prob_profit + prob_stop_terminal) × (exit_commission + exit_slippage) from ev_raw before the regime multiplier, and correct the docstring. Coordinate with the D21 re-baseline since it moves the CI-gated EV output.
- **Verifier:** Verified against the source. In EVEngine.evaluate, net_premium_in = gross_premium - entry_commission - entry_slippage (ev_engine.py:365) — exit legs are excluded. _compute_pnls (called at line 391, defined at 695-706: pnl = net_premium_in - buyback_cost) uses only that entry-net premium, and ev_raw = mean(pnls) (line 421). exit_commission and exit_slippage (lines 358-360, exit_slippage = entry_sli

## [MEDIUM] engine/ibkr_portfolio_adapter.py:790
**risk_view() reads net_liquidation with raw float() (not the module's _num guard) → /api/portfolio/risk 500s on a null/absent NAV, silently swapping live risk gates for MOCK breaches**

- **Category:** silent-wrong-risk-display  |  **Area:** dashboard-api
- **Failure scenario:** The module docstring and risk_view's own margin block (lines 855-859) state that a performance-report / PDF import carries JSON null for balance-sheet fields, which is why account_summary (l.554), build_holdings_view (l.260), build_positions_flat (l.378) and build_portfolio_context (l.474) all read net_liquidation through _num(). risk_view alone does `nav = float(acct["net_liquidation"])`. With a schema-valid snapshot whose account has net_liquidation: null (or the key omitted — load_snapshot only checks that 'account'/'positions' exist), float(None) raises TypeError / KeyError. _handle_portfolio_view (engine_api.py l.1436) catches ONLY SnapshotSchemaError, so /risk returns an uncaught 500 while /summary and /positions return 200. In use-portfolio-data.ts the risk slice then rejects and falls back to MOCK_DATA (concentration, single-name meters, R7-R10 gates, sector/currency, margin) — the operator sees hardcoded demo breaches (CLS 147%, sector 269%) overlaid on their real book. It carries a 'Mock' badge, but the page header can still read 'Partial — mixed sources' while every risk number shown is fabricated demo data.
- **Fix sketch:** Use the same defensive accessor as everywhere else: `nav = _num(acct.get("net_liquidation"))`. Optionally have _handle_portfolio_view catch a broader adapter error and return 503 (mock fallback) rather than letting a bare TypeError escape as a 500.
- **Verifier:** CONFIRMED and reachable. engine/ibkr_portfolio_adapter.py:790 reads `nav = float(acct["net_liquidation"])` with a raw float() on a direct subscript, whereas every sibling reader guards the same field with the module's `_num` helper: lines 260, 378, 474 use `_num(snapshot["account"]["net_liquidation"])` and 554 uses `_num(acct.get("net_liquidation"))`. `_num` (line 195) maps None/unparseable/non-fi

## [MEDIUM] `INV` engine/portfolio_risk_gates.py:870
**check_var silently falls through to delta-normal parametric VaR when correlation_matrix is supplied without volatilities, contradicting its stated no-silent-substitution contract**

- **Category:** missing-data-semantics  |  **Area:** risk-gates
- **Failure scenario:** check_var's skip guard is `if correlation_matrix is None and returns_data is None` (line 870), and the docstring (lines 846-856) promises it will NOT silently fall through to the delta-normal approximation (D11). But calculate_var's Priority-1 covariance path requires BOTH correlation_matrix AND volatilities to be non-None (risk_manager.py:405). PortfolioContext carries correlation_matrix and volatilities as independent Optionals (portfolio_risk_gates.py:102-103), so a caller that attaches a correlation_matrix but no per-ticker volatilities passes the guard, skips the covariance path (volatilities None), skips historical (returns_data None), and lands in _parametric_var — the exact single-factor delta-normal fallback the docstring says it avoids. For a concentrated book in prod that path returns (0.0, 0.0) (var_fallback_blocked), so var_pct=0, the gate passes, and R7 never fires despite real tail risk. The audit trail records a genuine VaR number/blocked-zero rather than the honest missing_data reason.
- **Fix sketch:** Tighten the guard so the covariance path is only claimed when its inputs are actually present: skip (missing_data) unless (correlation_matrix is not None and volatilities is not None) or returns_data is not None. I.e. treat correlation_matrix-without-volatilities as missing evidence, not as a reason to run parametric.
- **Verifier:** Confirmed real and reachable. check_var's skip guard (portfolio_risk_gates.py:870) is `correlation_matrix is None and returns_data is None`, but calculate_var's Priority-1 covariance path (risk_manager.py:405) requires BOTH correlation_matrix and volatilities non-None. When a caller supplies correlation_matrix but no volatilities (and no returns_data), the gate passes its guard, then calculate_var

## [MEDIUM] engine/stress_testing.py:467
**Monte Carlo t-distribution is not variance-normalized, inflating all tail-risk metrics ~29%**

- **Category:** scenario-correctness  |  **Area:** stress
- **Failure scenario:** z = stats.t.rvs(df=5, ...) then spot_change = z * daily_vol * sqrt(horizon_days). A Student-t with df=5 has variance df/(df-2)=5/3, so std=1.29, not 1. Every simulated horizon return is ~29% wider than the IV-implied target. With avg_iv=0.20, horizon=30d, intended horizon std=6.9% but realized simulation std=8.9%. Consequently monte_carlo_stress returns std, var_95, var_99, cvar_95, max_loss and prob_10pct_loss all systematically overstated by ~29%, biasing any risk report or sizing that consumes them.
- **Fix sketch:** Scale to unit variance: z = stats.t.rvs(df) * sqrt((df-2)/df) (df>2), so the t provides fat tails without inflating variance beyond the IV-implied scale.
- **Verifier:** Confirmed against engine/stress_testing.py:463-508. Line 467 draws z = float(stats.t.rvs(df, ...)) with df hardcoded to 5 (line 466), and line 470 computes spot_change = z * daily_vol * np.sqrt(horizon_days) where daily_vol = avg_iv/sqrt(252) (line 469). A Student-t with df=5 has variance df/(df-2) = 5/3, so std ≈ 1.291, not 1. There is NO variance normalization (no sqrt((df-2)/df) factor) anywher

## [MEDIUM] engine/stress_testing.py:798
**greeks_scenario_matrix delta_dollars multiplies aggregate delta by only positions[0]'s spot**

- **Category:** silent-wrong  |  **Area:** stress
- **Failure scenario:** total_delta is summed over ALL positions (delta * contracts*100*direction across symbols), but delta_dollars = total_delta * spot_prices.get(positions[0]['symbol'], 100). For a two-name book (e.g. AAPL spot 190 and a low-price name spot 20), the aggregate dollar delta is computed as if every share traded at the first symbol's price, producing a materially wrong portfolio dollar-delta in the greeks_surface DataFrame that downstream risk dashboards/memos display as authoritative. Also falls back to 100 when positions[0]'s spot is missing.
- **Fix sketch:** Compute dollar delta per position and sum: sum_i(greeks_i['delta'] * multiplier_i * new_spot_i), rather than multiplying the pooled delta by a single symbol's spot.
- **Verifier:** The computation at engine/stress_testing.py:798 is genuinely wrong. total_delta (line 789) is `sum over ALL positions of greeks["delta"] * multiplier`, aggregating across symbols. delta_dollars then multiplies that pooled delta by only `spot_prices.get(positions[0]["symbol"], 100)`. For a heterogeneous book (e.g. AAPL@190 + a low-price name@20) this dollarizes every position's delta at the first s

## [MEDIUM] `INV` engine/transaction_costs.py:147
**Entry fill assumed at mid − 0.15×spread (far better than the bid) manufactures edge that scales with spread width**

- **Category:** fill-realism  |  **Area:** costs
- **Failure scenario:** calculate_slippage returns spread_slippage = bid_ask_spread * base_factor with base_factor=0.15 for a liquid option (OI>500, spread_pct<0.30). Since bid_ask_spread is the FULL spread (ask−bid) and slippage is subtracted from mid, the assumed sell fill is mid − 0.15×spread, i.e. it captures 70% of the half-spread — much better than crossing to the bid (mid − 0.50×spread), which is the realistic marketable fill for an EOD wheel entry. The understatement is proportional to spread width, so a high-IV name with a $0.40 spread gets ~$0.14/share of manufactured edge vs the $0.20 a bid-cross would cost, while a tight $0.05-spread name gets almost none. Across a ranking this systematically inflates EV for wide-spread / illiquid / high-IV candidates relative to tight ones, distorting the top-N selection toward exactly the names where real fills are worst.
- **Fix sketch:** Default the spread-fraction to something bid-realistic (≈0.5 for taker fills, or make it the configurable slippage_pct_of_spread from finding 1) and document the resting-limit-at-mid assumption explicitly if 0.15 is intentional. Consider modeling entry as a draw between mid and bid rather than a fixed fraction.
- **Verifier:** Reachable and factually accurate about the code. EVEngine.evaluate (engine/ev_engine.py:350) calls calculate_slippage(trade_direction="sell", mid_price=trade.premium), and its result subtracts into net_premium_in (line 365), directly feeding EV — no guard blocks the path. In transaction_costs.py, for a liquid option (OI>500 skips lines 129-135; spread_pct<0.30 skips lines 140-143), base_factor sta

## [MEDIUM] `INV` engine/wheel_runner.py:3324
**F4 realized-vol tail widening is wired only into the short-put ranker; the covered-call and strangle rankers evaluate the un-widened forward distribution**

- **Category:** tail-risk-gap  |  **Area:** forward-dist
- **Failure scenario:** The put ranker `rank_candidates_by_ev` widens the forward distribution before EV eval: `fwd_rets_widened = realized_vol_widened_log_returns(fwd_rets, ohlcv, as_of=as_of)` (wheel_runner.py:2287-2298). The covered-call ranker (import block at 2938 pulls only `best_available_forward_distribution` + `is_iid_forward_source`, NOT the widening functions) feeds the RAW `fwd_rets` to `ev_eng.evaluate(..., forward_log_returns=fwd_rets)` at line 3324. The strangle ranker does the same at lines 4035/4041 (import block 3584 also omits the widening functions), and the tracker roll-EV `_fwd_for` (wheel_tracker.py:2625) likewise omits it. Concrete trigger: a name in a vol-cluster regime where rv30/rv252 >= 1.30 (per docs/F4_TAIL_RISK_DIAGNOSTIC.md that is ~14% of 2022-2024 dates; e.g. UNH 2024-11-11 ratio 1.36). On the put path that name's spread is widened up to x1.15, cutting EV (UNH example: $114.53 -> $108.25). On the covered-call and strangle paths the SAME regime produces zero widening, so the concave short-option payoff EV is over-stated and the loss tail (CVaR) is under-represented in exactly the elevated-vol regime the F4 fix was built to protect. A short strangle (short put + short call, the most concave/most tail-exposed position) is the one most helped by widening and it is the one that skips it entirely: `composed_ev = put_res.ev_dollars + call_res.ev_dollars` (line 4054) is computed on un-widened legs, so a strangle can clear `min_ev_dollars` or out-rank a widened put during the vol cluster. Test coverage confirms the gap: tests/test_f4_rv_widening.py::TestF4CasesRanker exercises only `rank_candidates_by_ev`.
- **Fix sketch:** Import `realized_vol_widened_log_returns` in the covered-call and strangle ranker import blocks (2938, 3584) and in wheel_tracker `_fwd_for`, then widen the returned `fwd_rets` (once per DTE, cached) before every `ev_eng.evaluate` call, mirroring the put path at 2287. Because the F4 widening is downgrade-only (factor >= 1.0, mean-preserving) it cannot rescue a candidate, so applying it uniformly is invariant-safe; add a strangle/covered-call regression case to test_f4_rv_widening.py to lock it.
- **Verifier:** All four cited locations verified in source. Put ranker rank_candidates_by_ev (wheel_runner.py:1272) imports realized_vol_widened_log_returns (1373) and widens the forward distribution at 2287 before evaluate (2296-2302). The covered-call ranker rank_covered_calls_by_ev (2824) import block (2938-2941) pulls only best_available_forward_distribution + is_iid_forward_source, omitting the widening fun

## [MEDIUM] `INV` engine/wheel_runner.py:2029
**Non-PIT option-chain overlays leak future data into ev_dollars in Theta backtests**

- **Category:** lookahead-leak  |  **Area:** wheel-runner
- **Failure scenario:** In `rank_candidates_by_ev` the chain is fetched with `conn.get_options(ticker)` / `conn.get_option_chain(ticker)` (lines 2028-2031) passing NO `as_of`. `ThetaConnector.get_option_chain` (theta_connector.py:452) takes no as_of and hits live `/v3/option/snapshot/*` endpoints, returning TODAY's chain. That chain drives `skew_mult` (skew_slope on 25d/ATM strikes, [0.85,1.08]), the dealer-positioning `market_structure` (dealer_mult [0.70,1.05], GEX/walls), and `strike_oi` (feeds slippage/cost). All of these scale `ev_dollars` and the cost model, so on a Theta backtest with a past `as_of` the EV number and ranking are contaminated with future chain data — while every other input (spot, IV, dividend, risk-free, news, credit, forward distribution, and even the dealer analyzer's time-to-expiry at line 2266 which IS given as_of) is PIT-gated. Internally inconsistent: the analyzer decays greeks to the as_of expiry off a live-snapshot chain. On the default Bloomberg connector this is dormant (no get_options/get_option_chain method) so the leak is Theta-path-only.
- **Fix sketch:** When `as_of is not None`, skip the chain fetch / overlays unless the connector advertises PIT-chain capability (e.g. a `supports_pit_chain` flag or a chain method that accepts as_of). Otherwise leave `chain_df=None` so skew/dealer/OI degrade to their neutral defaults rather than silently pricing off a future chain — drop-only, consistent with the overlays being multipliers not gates.
- **Verifier:** Traced the full call path and found no guard that makes the leak unreachable on the Theta provider.

1) Connector selection is real and documented: wheel_runner.py:899-903 selects `ThetaConnector` when `SWE_DATA_PROVIDER=theta`. Not a hypothetical — it's one of the two first-class providers.

2) Backtests call the ranker with a PAST as_of: backtests/regression/_common.py:629 calls `runner.rank_can

## [MEDIUM] engine/wheel_tracker.py:1567
**handle_call_assignment / handle_call_expiration hardcode 100 shares, over-selling phantom shares and overstating cash + realized P&L for any partial-lot position**

- **Category:** pnl-accounting  |  **Area:** tracker
- **Failure scenario:** A WheelPosition can hold stock_shares != 100: handle_partial_put_assignment sets stock_shares to shares_assigned (e.g. 50), and handle_partial_call_assignment reverts to STOCK_OWNED with remaining_shares (e.g. 40). open_covered_call (L1436) only checks state==STOCK_OWNED, not share count, so a covered call can be sold on a partial lot. When that call finishes ITM, handle_call_expiration (L1608) calls handle_call_assignment, which computes `stock_proceeds = pos.call_strike * 100` (L1567), `calculate_assignment_costs(..., shares=100)` (L1568) and `stock_pnl = (pos.call_strike - pos.stock_basis) * 100` (L1574) — all hardcoded to 100. Example: partial put assignment of 50 shares at basis 100, covered call strike 110, stock finishes at 115 → cash += 110*100 - fee = ~11000 and realized_pnl += (110-100)*100 = 1000, but only 50 shares existed; correct values are ~5500 and 500. The position is then finalized/deleted, so the phantom 50 shares are silently sold and the closed-trade net_pnl is overstated 2x. No test exercises partial-lot -> full call assignment, so it is unguarded.
- **Fix sketch:** Replace the hardcoded 100 in handle_call_assignment with `n = pos.stock_shares`: stock_proceeds = call_strike * n, calculate_assignment_costs(strike_price=call_strike, shares=n), stock_pnl = (call_strike - stock_basis) * n. handle_call_expiration should likewise route to handle_partial_call_assignment(shares_called=pos.stock_shares) so all-shares-called-away closes correctly for any lot size.
- **Verifier:** Confirmed real and reachable via the tracker's public API. handle_partial_put_assignment (engine/wheel_tracker.py L1051-1052) sets state=STOCK_OWNED with pos.stock_shares=shares_assigned (e.g. 50). open_covered_call (L1436) guards ONLY on `pos.state != STOCK_OWNED` with no stock_shares check, so a covered call can be opened on a partial lot. handle_call_expiration (L1608) routes ITM calls to handl

## [MEDIUM] `INV` engine/wheel_tracker.py:702
**EV-authority token is consumed (single-use, discarded) BEFORE the duplicate-ticker / cash / D17 gates, so those rejections silently burn a valid token despite the documented 'retain for retry' contract**

- **Category:** authority-gate  |  **Area:** tracker
- **Failure scenario:** In open_short_put strict mode, _consume_ev_authority_token runs first (L703) and on success discards the token from _ev_authority_tokens and pops its payload (L640-641). Only afterward does the method check `if ticker in self.positions: return False` (L715), the Reg-T cash check (L723), and the D17 hard-blocks (L751-762, which return False on e.g. sector_cap_breach). So a valid, positive-EV token gets permanently burned whenever the open is refused for a reason unrelated to EV freshness — e.g. issue a good AAPL token, call open_short_put while AAPL is already open, or while cash < margin, or when the single-name cap is breached: consume returns True (token gone), then the method returns False with no position opened. The docstrings promise stale-EV rejections 'retain the token for retry' and the audit log implies a clean issue/consume trail, but here the token is stranded with an action='consume' log entry yet no position — the operator must re-run the ranker to retry. open_covered_call (L1420) has the identical ordering. This fails safe (never rescues a bad trade) but violates the retry/audit contract and can strand legitimate authority.
- **Fix sketch:** Make token consume the LAST gate before committing the position: move the duplicate-ticker, cash, and _evaluate_d17_hard_blocks checks ABOVE the _consume_ev_authority_token call, or on any post-consume failure path re-add the token+payload and append a distinct audit entry (e.g. reason='consume_rolled_back_open_refused') so single-use authority survives non-EV refusals exactly as stale_ev does.
- **Verifier:** Factual claims verified against engine/wheel_tracker.py. In _consume_ev_authority_token, the stale-EV check (L629, current_ev_dollars <= 0) and non-finite check (L618) return False BEFORE the discard, so those genuinely retain the token — matching the documented "retain for retry" contract. But on passing those checks, L640-641 discard the token and pop the payload, log action="consume", and retur

## [MEDIUM] engine_api.py:2355
**/api/tv/scan swallows ranker exceptions and returns an empty 200, masking an engine failure as "no setups today"**

- **Category:** error-handling  |  **Area:** api-surface
- **Failure scenario:** In _handle_tv_scan the EV ranker call is wrapped in `except Exception: traceback.print_exc(); df = None` (lines 2347-2357). If `rank_candidates_by_ev` raises for any reason (bad data frame, connector hiccup, a bug in a downstream gate), the handler falls through to the `df is None or df.empty` branch and returns HTTP 200 with `{"signals": [], "count": 0, "authority": "ev_ranked"}` — byte-for-byte identical to a legitimate 'the universe genuinely produced zero candidates' response. An operator polling /api/tv/scan sees an empty scan, concludes the market has no wheel setups, and skips trading — when in fact the engine errored and real positive-EV candidates were never evaluated. This is inconsistent with the sibling trade endpoints /api/candidates (line 1058-1061) and /api/tv/ranked (line 2874-2877), which both call `_send_internal_error` (HTTP 500) on ranker failure, so the same underlying error is loud on one surface and silent-empty on another.
- **Fix sketch:** Remove the broad `except → df=None` swallow; let the exception propagate to the do_GET catch-all (which returns a 500 with a correlation id), or explicitly call `self._send_internal_error(exc, context="tv_scan:rank_candidates_by_ev")` and return, matching /api/candidates and /api/tv/ranked. Reserve the empty-200 path for a genuinely empty (non-error) result.
- **Verifier:** Confirmed against engine_api.py. In _handle_tv_scan the ranker call (lines 2348-2354) is wrapped in `except Exception: traceback.print_exc(); df = None` (2355-2357). Control then reaches `if df is None or df.empty:` (2359) and returns HTTP 200 with {"signals": [], "count": 0, "authority": "ev_ranked", "layer": "visual_context_only"} (2360-2368) — byte-identical to a legitimately empty universe. Re

## [MEDIUM] engine_api.py:1034
**/api/candidates silently resets ALL numeric params to defaults when any single one is malformed, discarding the operator's min_ev / min_score risk filters**

- **Category:** input-validation  |  **Area:** api-surface
- **Failure scenario:** _handle_candidates parses limit/dte/delta/min_ev/min_score inside one shared `try` block (lines 1034-1041); the `except (TypeError, ValueError)` clause reassigns EVERY parsed value to hardcoded defaults `15, 35, 0.25, 0.0, 0.0`. So a request like `GET /api/candidates?min_ev=200&min_score=70&delta=xyz` (a fat-fingered delta) does not 400 — it silently drops the operator's `min_ev=200` and `min_score=70` filters, resetting min_ev to 0.0 and min_score to 0.0, and returns the full unfiltered EV-ranked list as the AUTHORITATIVE tradeable candidate set (docstring: 'the single authoritative entry point for tradeable candidates'). The operator believes their strict EV/score floor is applied and reviews a flood of marginal candidates that should have been filtered out. This also diverges from the rest of the dispatcher, which routes params through `_parse_param` and returns a clean 400 (BadParam) on malformed input.
- **Fix sketch:** Parse each param independently (or route them through `_parse_param` like the other endpoints) so one bad value yields a 400 naming that param, and a good value is never silently overridden by an unrelated field's parse error. At minimum, do not reset the successfully-parsed filters (min_ev, min_score) because delta failed to parse.
- **Verifier:** Confirmed real and reachable by reading engine_api.py. The dispatcher (lines 750-759) routes /api/candidates by passing RAW query strings (via param()) straight to _handle_candidates without any _parse_param coercion or upstream validation, so malformed values do arrive unguarded. In _handle_candidates (lines 1034-1041) all five numeric params are parsed in ONE shared try block; the `except (TypeE

## [MEDIUM] scripts/_bbg_panel.py:165
**Per-chunk bdh exceptions are swallowed, silently dropping whole ticker chunks from the OHLCV/IV/liquidity monoliths with no run failure**

- **Category:** silent-truncation  |  **Area:** pull-scripts
- **Failure scenario:** During a refresh of sp500_ohlcv.csv/sp500_vol_iv_full.csv/sp500_liquidity.csv, one bad or renamed ticker in a 250-name chunk (or a transient Bloomberg error) makes blp.bdh raise for the entire chunk. `_pull_window` catches it at line 165-166 (`print(ERROR)`, continue), so all ~250 tickers in that chunk contribute ZERO rows for that window. `run()` still concats the surviving chunks, writes the CSV, and prints 'DONE ... N tickers' with a silently reduced ticker count and NO nonzero exit. plan_windows keys only off the GLOBAL date min/max, so those tickers' rows for that already-covered date range are never re-pulled on later runs — a permanent per-ticker hole. The empirical forward-distribution / block-bootstrap for those names then runs on fewer observations, biasing their EV, or the per-ticker staleness gate quietly drops them from the ranking universe.
- **Fix sketch:** Track per-chunk failures; after the window, if any chunk failed raise (or write a sidecar 'missing_tickers' manifest and exit nonzero) so a partial universe cannot masquerade as a clean 'DONE'. At minimum, count expected vs. realized tickers per window and fail loudly on a shortfall.
- **Verifier:** Confirmed and reachable. In scripts/_bbg_panel.py, _pull_window (lines 136-169) wraps each blp.bdh chunk call in `try/except Exception` at lines 145-166; on any exception it prints `ERROR chunk {i}` and continues, so all ~250 tickers in that chunk contribute zero rows for the window. Surviving chunks are concatenated (line 169) or, if all fail, None is returned. run() (lines 227-264) writes combin

## [MEDIUM] scripts/pull_ohlcv.py:67
**Hardcoded stale end_date default freezes the recent edge of every core panel; a 'refresh' run does backfill work but never advances to today**

- **Category:** date-window-staleness  |  **Area:** pull-scripts
- **Failure scenario:** end_date is hardcoded '2026-06-04' (pull_ohlcv.py:67; identically in pull_vol_iv, pull_liquidity, pull_iv_surface, and '2026-06-05' in pull_macro/pull_treasury_yields). Today is 2026-07-15. If the operator runs `python scripts/pull_ohlcv.py` to refresh without setting SWE_PULL_END, plan_windows computes the forward gap fstart = existing_max+1 = 2026-06-05 which is > end_date 2026-06-04, so NO forward window is planned. The backfill windows still run (walking toward 1994), so the operator sees a long, busy, successful run and assumes data is current — but the recent edge stays frozen at 2026-06-04, leaving ~6 weeks of missing current OHLCV/IV/rate/spot. The live engine then ranks candidates on 6-week-stale spot and IV. (Read-side staleness gates may block trading rather than mis-trade, but the pull silently fails to deliver fresh data.)
- **Fix sketch:** Default end_date to today (pd.Timestamp.today()) rather than a frozen literal, or hard-fail/warn when end_date is older than today minus a few trading days so a stale literal cannot silently cap the forward gap.
- **Verifier:** Confirmed against the code. pull_ohlcv.py:67 hardcodes end_date="2026-06-04" in PanelConfig. In _bbg_panel.py:175, run() resolves end_date = _env("SWE_PULL_END", cfg.end_date), so with no SWE_PULL_END the frozen literal is used. In plan_windows (_bbg_panel.py:113-117) the forward gap is only appended when fstart = emax + 1 day <= end_ts. Once the existing CSV's max date reaches the literal 2026-06

## [MEDIUM] tests/test_survivorship_harness.py:25
**Survivorship-bias guard (terminal_spot delisting-loss realization + pit_universe) has zero CI-executable coverage — every test skips without non-committed deep data**

- **Category:** test-coverage  |  **Area:** tests-meta
- **Failure scenario:** backtests/survivorship.py:terminal_spot() carries the load-bearing survivorship guarantee: for an assigned put on a name that delisted (e.g. Lehman crashing toward zero), it must NEVER return None — it returns the last close on/before expiry (the delisting price) so the loss is REALIZED, not silently dropped (which a plain on/after lookup does past the delisting date). pit_universe() likewise must return the point-in-time S&P 500 membership (including since-delisted names, excluding later joiners) rather than today's survivor-only list. Both test files (test_survivorship_harness.py, test_survivorship_r6_lehman.py) are gated on `pytest.mark.skipif(not SWE_DEEP_TEST_DATA ...)` and the deep/delisted panels are not committed, so 100% of survivorship coverage SKIPS in CI. Concrete regression that ships green: someone 'simplifies' terminal_spot to `return _spot_on_or_after(conn, ticker, expiry, max_lookahead_days), False` (dropping the lines 108-114 delisting branch). Now every delisted name past its delisting date returns (None, False); the assigned loss is silently dropped; any backtest run through run_survivorship_backtest reports inflated, survivorship-biased returns and the 2008 Lehman 'no silent drop' proof is quietly false — yet CI is fully green because both tests skipped. Same for a pit_universe regression that returns get_universe() (today's survivors) for a historical as_of. This is the classic survivorship leak the review explicitly flags as outranking loud crashes, and it is exactly the kind of guarantee that is trivially unit-testable but currently untested.
- **Fix sketch:** Add an UNGATED synthetic-connector unit test (no SWE_DEEP_TEST_DATA): (1) feed terminal_spot a stub connector whose get_ohlcv(ticker, end_date=...) returns a DataFrame ending BEFORE expiry with no on/after bar, and assert it returns (last_close, True) — never (None, _); add the truly-empty case asserting (0.0, True); add a live-name case with an on/after bar asserting (spot, False). (2) feed pit_universe a stub ConsolidatedBloombergLoader whose get_universe_as_of returns a fixed membership, asserting a historical as_of includes a since-delisted name and excludes a later-joiner. These pin the delisting-loss-realization and PIT-membership guarantees in the default lane so a revert trips CI without the deep data.
- **Verifier:** Confirmed by reading code and executing tests. backtests/survivorship.py:terminal_spot (lines 105-114) implements the load-bearing survivorship guarantee: on/after lookup → (spot,False); else the delisting fallback branch (108-114) returns the last close on/before expiry as (close,True); else (0.0,True) — never None. pit_universe (71-82) returns PIT membership via get_universe_as_of. Every test in

## [LOW] PROJECT_STATE.md:224
**PROJECT_STATE.md D17-status line pointer for _d17_gate_enabled is ~35 lines stale**

- **Category:** docs-drift  |  **Area:** docs-drift
- **Failure scenario:** PROJECT_STATE.md:224 anchors the D22 cap-arming logic to `engine.wheel_tracker._d17_gate_enabled, wheel_tracker.py:1963`. The method is actually defined at wheel_tracker.py:1998; line 1963 lands in the middle of an unrelated docstring about static-NAV fallback. An agent following the pointer to confirm whether require_ev_authority=True still arms the sector/single-name hard refusals (it does, via the `if self.require_ev_authority: return True` at line 2006) reads the wrong code and could mis-conclude the arming semantics. Same section's `engine_api.py:434-435` pointer for the `_build_portfolio_context_from_params` nav==None return-None also lands in the docstring; the function is at :409 and returns None at :446.
- **Fix sketch:** Update the anchors in PROJECT_STATE.md:224 (1963 -> 1998) and :239 (434-435 -> 446), or replace numeric line pointers with symbol names since these drift on every edit to wheel_tracker.py / engine_api.py.
- **Verifier:** Both line pointers are verified stale against the actual code. PROJECT_STATE.md:224 cites `wheel_tracker.py:1963` for `_d17_gate_enabled`, but line 1963 is a blank line inside the static-NAV fallback docstring (method docstring spans 1955-1974); the real method def is at line 1998 with `if self.require_ev_authority: return True` at line 2006 — exactly as the finding claims. PROJECT_STATE.md:239 ci

## [LOW] backtests/regression/_common.py:359
**spearman_p treats overlapping, daily-repeated candidate rows as i.i.d., producing meaningless near-zero p-values locked into snapshots**

- **Category:** metric-correctness  |  **Area:** backtests
- **Failure scenario:** Each ticker is ranked every trading day, and each day's row is a ~35-day forward-looking observation, so consecutive rows for the same name overlap almost entirely (highly autocorrelated). _spearman_rho_and_p feeds the full flat array to scipy.stats.spearmanr (line 336) which assumes independent observations, yielding p-values like ~1e-67 that are physically implausible for the true number of independent trades (~a few hundred non-overlapping windows). The snapshot locks spearman_p with rel 1% tolerance and the reliability studies lean on these as evidence the EV->realized edge is significant. A reader/operator over-trusts the engine's predictive edge because the reported significance is inflated by ~orders of magnitude.
- **Fix sketch:** Either compute the p-value on non-overlapping windows (one observation per ~35-day block) or via a block bootstrap (the module docstring already references block-bootstrap machinery), or stop reporting spearman_p and report only rho with a bootstrapped CI. Annotate the snapshot that p is descriptive, not inferential.
- **Verifier:** CONFIRMED and reachable. The overlapping input genuinely arrives: the backtest driver ranks every ticker on every business day (`for today in trading_days`, _common.py:517; `pd.bdate_range` at 513/986) and each row is a ~35-day forward observation (`dte_target: int = 35`, line 484; expiration at 568/638), so consecutive same-ticker rows overlap ~34/35 and are heavily autocorrelated. `_compute_metr

## [LOW] backtests/regression/_common.py:456
**Settlement and forward-replay value expirations at up to 7 calendar days AFTER expiry when the expiry-day price is missing (small lookahead in assignment decision and realized P&L)**

- **Category:** lookahead  |  **Area:** backtests
- **Failure scenario:** _spot_on_or_after defaults max_lookahead_days=7 and returns the first close in [target, target+7]. It is used to (a) settle expirations: spot = prices_today.get(t) or _spot_on_or_after(conn, t, today) (line 539/553, and survivorship.py terminal_spot line 105), which then decides was_assigned = spot < strike and drives handle_put_expiration P&L; and (b) forward-replay realized_pnl at exp_dt (line 693). When the exact expiry/settlement day has no bar (holiday-adjacent gap, illiquid or halted name), the position is settled/valued at a price up to 7 calendar days in the future. Concretely a put expiring on a day the underlying gapped down and then rebounded over the next week can be scored OTM (kept premium) using the later, higher close instead of the true expiry-day price, flipping an assignment to a win in both the tracker P&L and the realized metric.
- **Fix sketch:** For settlement/expiry valuation use the last close ON OR BEFORE expiry (backward fill), not a forward lookahead; reserve the forward on-or-after lookup only for opening/entry-day pricing. If forward fill is required for genuinely missing bars, cap it to 1 trading day and record the substitution.
- **Verifier:** The finding is confirmed and reachable. `_spot_on_or_after` (backtests/regression/_common.py:456-471) defaults max_lookahead_days=7 and returns df['close'].iloc[0], the first close in [target, target+7]; when the target day has a bar there is no lookahead, but a missing target-day bar makes it settle/value at a close up to 7 calendar days AFTER expiry. It is used with the default 7-day window for 

## [LOW] dashboard/src/components/portfolio/equity-curve.tsx:38
**Equity-curve 1D/1W tab silently contradicts the 1D/1W return KPI — the two are sourced from different pipelines, so the 'lockstep' comment is false for these windows**

- **Category:** display-inconsistency  |  **Area:** dashboard-api
- **Failure scenario:** returns_view() derives 1D/1W pct/usd from the snapshot's account deltas (adapter l.622-629: day_change_pct/week_change_pct), while EquityCurve's windowByDate() computes the 1D/1W view by slicing the equity curve at WINDOW_DAYS 1/7 days and anchoring to the last point strictly before that. On the demo (monthly curve) with period='1D': last date 2026-06-05, window_start 2026-06-04, anchor falls on the May 29 point, so the '1D' chart tab plots May 147200 → Jun 144507 (a −$2,693 / −1.8% move) while the '1D' KPI card shows −$1,240 / −0.85%. Even in the intended daily-append steady state the curve's last point 'lags the live NAV until the next append' (page.tsx l.156-160), so the chart's day move and the KPI day change still measure different things. The WINDOW_DAYS comment (l.32-37) asserts the chart and KPI stay 'in lockstep' and even has a TODO for a parity test — but that lockstep only exists for 1M/3M/YTD/1Y (both date-anchored off the curve); 1D/1W were never in lockstep.
- **Fix sketch:** Either dim/disable the 1D and 1W tabs on the chart (they have no curve-grain support), or drive the 1D/1W KPI from the same curve anchor the chart uses so both agree, and correct the misleading lockstep comment to exclude 1D/1W.
- **Verifier:** Confirmed and reachable through the real call path. engine/ibkr_portfolio_adapter.py::returns_view (l.587-628) derives 1M/3M/YTD/1Y from the equity curve via _window/_anchor_index, but 1D/1W are taken directly from the snapshot's account.day_change_pct/usd and week_change_pct/usd — they never go through the curve-anchoring path. EquityCurve.windowByDate (equity-curve.tsx l.46-66) windows ALL perio

## [LOW] `INV` engine/candidate_dossier.py:604
**R7/R8 price the candidate leg at spot=strike because the reviewer never passes the known ev_row['spot'] into check_var/check_stress_scenario**

- **Category:** risk-mispricing  |  **Area:** dossier-reviewers
- **Failure scenario:** PortfolioContext.spot_prices is built from the HELD book snapshot (take_snapshot). A freshly screened candidate ticker is by definition not yet held, so it is absent from ctx.spot_prices. R7 calls check_var(spot_prices=ctx.spot_prices, candidate_option=_build_candidate_dict(...)) (candidate_dossier.py:399-408) and R8 does the same for check_stress_scenario (432-439). _build_candidate_dict returns a dict with no 'spot' (lines 604-612), and the candidate's real spot — which IS present on the row as ev_row['spot'] (wheel_runner.py:2346) — is never forwarded. Inside the gate, full_spots.setdefault(symbol, strike) (portfolio_risk_gates.py:890-892 / 976-978) substitutes the option STRIKE for the underlying spot. For a 30-delta short put (strike ~ 0.9*spot) the candidate leg is repriced as near-ATM, so its marginal VaR/stress contribution is systematically misstated for essentially every not-yet-held candidate (the common case). Direction is usually over-conservative (ATM leg overstates delta/gamma/vega -> spurious R7/R8 downgrade), but under portfolio netting/correlation the mispriced marginal can move the pass/fail either way, so a genuine breach can also be missed.
- **Fix sketch:** In _build_candidate_dict include the candidate's real spot (spot = float(ev_row.get('spot', 0) or 0)), and at the R7/R8 call sites merge {dossier.ticker: spot} into the spot_prices passed to check_var/check_stress_scenario so the gate uses the known spot instead of the strike fallback.
- **Verifier:** Confirmed real and reachable. _build_candidate_dict (candidate_dossier.py:604-612) omits any 'spot' key while the true spot IS on ev_row['spot'] (wheel_runner.py:2346). R7 (line 402) and R8 (line 436) pass spot_prices=ctx.spot_prices plus the candidate dict, never merging {ticker: ev_row['spot']}. The gates then do full_spots.setdefault(p['symbol'], p['strike']) (portfolio_risk_gates.py:892 for Va

## [LOW] engine/candidate_dossier.py:272
**R11 rationale cites a nonexistent doc file inside the decision-trio source (candidate_dossier.py x2)**

- **Category:** docs-drift  |  **Area:** docs-drift
- **Failure scenario:** An auditor verifying that the live R11 elevated-vol size-down downgrade (a rule that alters verdicts on real candidates) rests on sound calibration follows the in-source citation `See docs/HEAVY_VERIFY_2026-05-31_I11.md` at candidate_dossier.py:272 (and again at :555 in the R11 code comment). That file does not exist — the actual study is docs/HEAVY_VERIFY_2026-05-31_I11_RISK_BUDGET_STUDY.md (plus a _SPEC.md). The '~0.57 realized vs ~0.96 forecast' and 'VIX>25 survives leave-one-crisis-out' claims that justify a downgrade-only trading rule are therefore unverifiable from the pointer given. No wrong trade results (downgrade-only, conservative), but the audit trail for an invariant-adjacent rule is broken.
- **Fix sketch:** Repoint both citations (candidate_dossier.py:272 and :555) to docs/HEAVY_VERIFY_2026-05-31_I11_RISK_BUDGET_STUDY.md; grep the tree for other HEAVY_VERIFY_2026-05-31_I11.md references (CLAUDE.md-style docs) and fix them too.
- **Verifier:** Confirmed the core defect: candidate_dossier.py:272 contains `See ``docs/HEAVY_VERIFY_2026-05-31_I11.md``` and `ls docs/` shows that exact filename does NOT exist — the real files are HEAVY_VERIFY_2026-05-31_I11_RISK_BUDGET_STUDY.md and ..._SPEC.md. So the in-source citation points to a nonexistent file; that is a genuine docs-drift defect, correctly rated low. It affects an invariant-adjacent but

## [LOW] engine/data_connector.py:1565
**get_fundamentals serves current-snapshot implied_vol_atm/beta/sector at any historical as_of; only dividend_yield is PIT**

- **Category:** pit-lookahead  |  **Area:** connector-data
- **Failure scenario:** get_fundamentals makes only `dividend_yield` point-in-time (line 1567-1570); `implied_vol_atm` (line 1565), `beta`, `market_cap`, `sector`, and `volatility_30d` are always the current (2026) snapshot regardless of as_of. In the puts ranker, when `_resolve_pit_atm_iv` returns None (a name/date with no get_iv_history coverage — common for thin or early history), the code falls back to `fundamentals['implied_vol_atm']` (wheel_runner.py:1672), i.e. today's ATM IV, to price a historical trade. A 2019 backtest bar with missing IV history is then priced with 2026 implied vol — a forward-looking volatility input to EVEngine.evaluate that biases EV and ranking. `sector` from the current snapshot is likewise used by the R9 sector-cap gate for historical membership. Documented as 'pending broader PIT wiring' but still an active lookahead on the fallback path.
- **Fix sketch:** Gate implied_vol_atm behind as_of like dividend_yield (serve NaN, forcing the ranker's degenerate-IV drop, rather than a future snapshot IV when no PIT IV exists), or at minimum refuse the fundamentals IV fallback when as_of is set and no dated IV is available. Same treatment for sector when feeding PIT gates.
- **Verifier:** Confirmed real and reachable. data_connector.py:1552-1571: get_fundamentals's as_of block (1567-1570) overrides ONLY dividend_yield via _pit_dividend_yield; implied_vol_atm (1565), beta, market_cap, sector, volatility_30d are read from a single undated fundamentals CSV row (current snapshot) regardless of as_of — the docstring explicitly states 'ONLY dividend_yield is made PIT here (the other fiel

## [LOW] `INV` engine/forward_distribution.py:308
**HAR-RV forward tier is strictly zero-drift while empirical and block-bootstrap tiers carry the historical drift, biasing short-call EV upward on short-history names**

- **Category:** model-inconsistency  |  **Area:** forward-dist
- **Failure scenario:** `har_rv_conditional_distribution` builds paths from a symmetric zero-mean Student-t: `z = rng.standard_t(df_t, ...)`, `daily_sim = sigma_daily_fc * z`, `horizon_log_rets = daily_sim.sum(axis=1)` (lines 302-308) -> the horizon distribution has mean exactly 0 (no drift). The empirical tiers carry realized drift (mean of actual forward log-returns) and the block bootstrap carries drift (sums actual daily returns including their mean). So which tier services a candidate silently changes its assumed drift by ~equity-drift-over-horizon (~0.8% over 35d). Dropping the positive equity drift understates the probability of an UP breach, which OVER-states the EV of a short call (covered-call leg and the call leg of a strangle: less mass above strike -> lower expected buyback cost -> higher EV) -- an anti-conservative direction, not merely a conservative one. Concrete trigger: a recently spun-off/added S&P name with 60-94 trading bars evaluated on a research/backtest path with enforce_history_gate=False, or in the tracker roll-EV `_fwd_for` which applies no history gate: NOS (needs ~735 bars) and overlapping (needs >=95 bars) and block-bootstrap (needs >=100 bars) all fail, so `best_available_forward_distribution` falls through to `har_rv` and the covered-call/strangle EV for that name is computed against a driftless distribution unlike its full-history peers. Reachability is limited because the default production rankers gate at min_history_days=504, which blocks har_rv/block_bootstrap on the default entry path -- hence low severity.
- **Fix sketch:** Add the historical daily-drift back to the HAR paths (e.g. `daily_sim = mu_daily + sigma_daily_fc * z`, with mu_daily = mean of the PIT daily log-returns) so the HAR tier is drift-consistent with the empirical/bootstrap tiers; or document har_rv as an intentionally risk-neutral tier and exclude it from the covered-call/strangle rankers. Add a unit test asserting the har_rv horizon mean is within a tolerance of horizon*mu_daily.
- **Verifier:** Code verified. The three tiers are drift-inconsistent as claimed: (1) empirical_forward_log_returns (forward_distribution.py:133) returns actual forward log-returns carrying realized drift; (2) block_bootstrap_log_returns (line 202) sums actual daily_rets which have non-zero mean; (3) har_rv_conditional_distribution (lines 302-308) builds paths from a symmetric variance-rescaled Student-t with no 

## [LOW] engine/forward_distribution.py:187
**block_bootstrap and har_rv compute log-returns without the isfinite guard the empirical path applies; a corrupt close silently shrinks scenario count or falls to lognormal under an unchanged method label**

- **Category:** data-robustness  |  **Area:** forward-dist
- **Failure scenario:** `empirical_forward_log_returns` deliberately filters `log_rets = log_rets[np.isfinite(log_rets)]` (line 138) and `realized_vol_ratio` explicitly guards the non-finite case (lines 513-521, with a comment that a non-positive close survives dropna and poisons np.log). But `block_bootstrap_log_returns` (`daily_rets = np.diff(np.log(prices))`, line 187) and `har_rv_conditional_distribution` (line 259) never filter isfinite. Concrete trigger: a single zero or negative close in the PIT window (corrupt bar the data layer is supposed to pin away but this module does not assume so elsewhere) makes np.log produce -inf and np.diff produce nan/inf inside `daily_rets`. In the bootstrap loop `total += daily_rets[idx]` then contaminates every scenario that samples that index; over 2000 scenarios x horizon draws a large fraction become non-finite. Downstream `EVEngine._build_terminal_prices` filters `arr = arr[np.isfinite(arr)]` (ev_engine.py:667), so the poisoned scenarios are dropped, silently shrinking N (skewing prob_profit) or, if all are poisoned, falling through to the lognormal fallback -- while the ranker still records `method='block_bootstrap'`/`'har_rv'` (a provenance/N mismatch). Same reachability caveat as the HAR finding (gated off the default entry path; reachable in research/tracker paths).
- **Fix sketch:** After computing `daily_rets` in both samplers, drop non-finite entries (`daily_rets = daily_rets[np.isfinite(daily_rets)]`) and re-check the length guards, matching the empirical path; alternatively filter non-positive closes before np.log. This keeps method-label provenance honest and prevents silent N shrinkage.
- **Verifier:** All factual claims verified against source. engine/forward_distribution.py:138 (empirical) and lines 510-521 (realized_vol_ratio) both explicitly filter/guard non-finite values arising from a zero/negative close surviving dropna and poisoning np.log — with comments naming the exact mechanism and the data-layer test (test_ohlcv_prices_positive). By contrast, block_bootstrap_log_returns (line 187, d

## [LOW] engine/ibkr_portfolio_adapter.py:172
**provenance() labels any snapshot without an explicit 'source' marker as 'Live IBKR' with no staleness guard — a stale or partially-regenerated drop reads as live**

- **Category:** stale-data-display  |  **Area:** dashboard-api
- **Failure scenario:** provenance() returns 'live' for any artifact not explicitly marked fixture/demo/mock. The Dashboard terminal regenerates data_processed/ibkr/*.json from IBKR (gitignored). If a refresh partially fails or is days old but still writes a valid snapshot (no 'source' key), every slice reports source='live', provenanceSummary() returns 'Live IBKR', and the only freshness cue is the account.asOf string — there is no max-age / staleness check like the cockpit's staleDays. An operator glancing at a green 'Live IBKR' header can act on concentration/margin numbers that are hours or days stale without a warning.
- **Fix sketch:** Add an as_of-age check: if the snapshot's as_of is older than a threshold (e.g. > 1 trading day), downgrade the badge to a 'Stale' tone or surface an explicit staleness warning in provenanceSummary, rather than treating absence-of-marker as unconditional 'live'.
- **Verifier:** Confirmed against the code. provenance() at engine/ibkr_portfolio_adapter.py:172-173 lower-cases artifact["source"] and returns "demo" only for fixture/demo/mock; every other input, including an absent source key, returns "live". The function never references as_of. load_snapshot (lines 126-136) validates only schema_version==1 and presence of account/positions — no freshness/max-age check exists 

## [LOW] engine/portfolio_copula.py:242
**Copula tail_amplification masks tail dependence to 'negligible' when Gaussian CVaR <= 0 and explodes when it is near zero**

- **Category:** correctness-edge-case  |  **Area:** tail-copula
- **Failure scenario:** portfolio_cvar_copula computes `amp = t_cvar / g_cvar if g_cvar > 0 else 1.0` (portfolio_copula.py:242) and drives the verdict ladder off amp (lines 243-250). For a wheel book whose 5% worst weighted outcome is still a net gain (small notional relative to premium, or low correlation with positive drift), the Gaussian CVaR g_cvar is <= 0, so amp is forced to 1.0 and verdict='negligible_tail_dependence' regardless of how fat the t-copula joint tail actually is -- the exact scenario the module exists to catch is silently reported as safe. Symmetrically, when g_cvar is a tiny positive value the ratio blows up and reports a spurious 'critical_tail_dependence'. Because this report is advisory (portfolio_cvar_copula is not wired into EVEngine.evaluate or any R1-R11 gate), the impact is a wrong tail-dependence risk number shown to the operator rather than a wrong trade. Note also df is hardcoded to 5.0 (no estimation despite the 't-copula df estimation' intent), so the verdict is largely a function of the assumed df and the correlation matrix, not the data's actual tail dependence.
- **Fix sketch:** Compare tail dependence on an absolute basis when g_cvar is non-positive or near zero: e.g. report the additive gap (t_cvar - g_cvar) alongside the ratio, guard the denominator (max(g_cvar, eps) plus an explicit 'gaussian_tail_nonpositive' verdict), and either estimate df from the joint exceedances or clearly label the output as an assumption-comparison at fixed df=5.
- **Verifier:** The code at engine/portfolio_copula.py:242 reads exactly `amp = t_cvar / g_cvar if g_cvar > 0 else 1.0`, and lines 243-250 drive the verdict ladder purely off `amp`. Both failure modes are real and reachable, no guard prevents them: (1) when g_cvar <= 0, amp is unconditionally forced to 1.0, yielding verdict='negligible_tail_dependence' regardless of the actual t-copula tail; (2) when g_cvar is a 

## [LOW] engine/portfolio_risk_gates.py:698
**check_portfolio_delta under-counts portfolio delta for STOCK_OWNED holdings when spot price is absent, weakening the hard delta cap**

- **Category:** notional-math  |  **Area:** risk-gates
- **Failure scenario:** Stock-leg delta-dollars is `shares * full_spots.get(ticker, 0.0)` (lines 697-699). full_spots is seeded from the caller's spot_prices and then setdefault-ed only from OPTION positions' strikes (line 689-690). A STOCK_OWNED position (assigned shares, no live option leg) contributes no option row, so its ticker enters full_spots only if the caller supplied a spot. The tracker builds the context with spot_prices defaulting to `{}` when None (wheel_tracker.py:1910-1911) and passes it straight through, so a held assigned position whose ticker is missing from spot_prices gets spot=0.0 -> 0 stock delta. That undercounts the true long-100-delta-per-share exposure, so `abs(portfolio_delta_dollars) <= cap_dollars` can pass when the real book is over the delta cap. Because this is a refuse-only hard block, the effect is a silently-permissive gate (fewer refusals) rather than a crash.
- **Fix sketch:** Require an explicit spot for every held/candidate ticker (raise or return missing_data when a stock holding has no spot) instead of silently substituting 0.0; at minimum log/flag the substitution so the delta number isn't silently understated.
- **Verifier:** The code defect is real: in check_portfolio_delta (engine/portfolio_risk_gates.py:697-699), stock-leg delta-dollars is `shares * full_spots.get(ticker, 0.0)`, and full_spots is only setdefault-seeded from OPTION positions' strikes (line 690). A pure STOCK_OWNED holding with no live option leg has no strike fallback, so an absent spot yields 0.0 stock delta. That undercounts long stock delta, shrin

## [LOW] engine/portfolio_tracker.py:1259
**PortfolioTracker.export_to_json shares the same non-atomic truncate-on-open write for cash/holdings/realized-P&L state**

- **Category:** non-atomic-file-write  |  **Area:** concurrency-state
- **Failure scenario:** export_to_json(filepath) opens the destination with mode 'w' (immediate truncation) and streams json.dump of cash, holdings, transactions, snapshots, realized_pnl, total_fees/dividends/deposits/withdrawals with default=str. It is invoked live from the dashboard (dashboard/quant_dashboard.py:1164 _tracker_export). An interrupt or disk-full mid-write, or overwriting the same portfolio.json path while another process reads it, corrupts/erases the file; load_from_json then does data['cash'] against a truncated file -> JSONDecodeError, and the prior portfolio state (realized P&L, holdings, deposit/withdrawal ledger used to derive NAV) is lost — a wrong NAV feeds every percentage-of-NAV risk cap.
- **Fix sketch:** Same temp-file + os.replace atomic-rename pattern; write to filepath+'.tmp', fsync, then os.replace onto filepath so a partial write can never clobber the last good export.
- **Verifier:** Code confirmed as described. engine/portfolio_tracker.py:1259 `with open(filepath, "w") as f: json.dump(data, f, indent=2, default=str)` truncates the destination on open and streams cash/holdings/transactions/snapshots/realized_pnl/total_* directly — no temp file, no fsync, no os.replace. An interrupt or disk-full mid-write, or overwriting the path while another process reads it, leaves a partial

## [LOW] engine/stress_testing.py:308
**run_scenario ignores per-position rate, diverging from every greeks_* method**

- **Category:** consistency  |  **Area:** stress
- **Failure scenario:** run_scenario prices both current and shocked legs with self.risk_free_rate (0.05), never reading pos['rate']. Every other method (greeks_stress_ladder:568, greeks_scenario_matrix:728/773/822, extreme_greeks_scenarios:970/984) uses pos.get('rate',0.05). A position carrying rate=0.03 is priced at 0.05 in run_scenario but 0.03 elsewhere, so the same position yields different scenario P&L depending on which stress function is called. Impact is small because the rate largely cancels in the differenced P&L, but it is an unnecessary inconsistency.
- **Fix sketch:** Have run_scenario read r = pos.get('rate', self.risk_free_rate) for the base leg and apply the rate shock on top, matching the greeks_* methods.
- **Verifier:** Factually confirmed: engine/stress_testing.py run_scenario prices the base leg at r=self.risk_free_rate (line 308) and the shocked leg at new_rate=self.risk_free_rate+shock (lines 298/326), never consulting pos['rate']. Every sibling method uses pos.get('rate',0.05): greeks_stress_ladder:568, greeks_scenario_matrix:728/773/822, extreme_greeks_scenarios:970/984. So a position with rate!=0.05 yields

## [LOW] `INV` engine/wheel_runner.py:2536
**rank_candidates_by_ev returns a column-less empty DataFrame, breaking its documented schema contract**

- **Category:** schema-inconsistency  |  **Area:** wheel-runner
- **Failure scenario:** When every candidate is gated out (e.g. all tickers fail the 504-day history gate, or all fail the staleness/strike/premium gates), `rows` is empty and `df = pd.DataFrame(rows)` (line 2536) produces a DataFrame with NO columns. The docstring promises the frame 'Always carries the capital-efficiency columns collateral and roc' plus ev_dollars/ev_per_day. The sibling rankers avoid this by pinning `pd.DataFrame(rows, columns=cols)` (rank_covered_calls_by_ev:3450, rank_strangles_by_ev:4166), but the put ranker has no pinned column list, so an external consumer doing `df['ev_dollars']` / `df['collateral']` on an all-dropped result hits a KeyError instead of an empty typed column. Internal consumers (select_book, build_candidate_dossiers, consume_into_tracker) happen to guard `len(df)==0` first, so it is latent today, but it violates the stated contract and diverges from the other two rankers.
- **Fix sketch:** Pin an explicit core (+ diagnostic) column list for the put ranker mirroring `_CC_RANK_CORE_COLUMNS`, and build the empty/full frame with `pd.DataFrame(rows, columns=cols)` so the empty path returns a correctly-shaped zero-row frame carrying collateral/roc/ev_dollars/ev_per_day.
- **Verifier:** Verified in engine/wheel_runner.py. Line 2536 builds `df = pd.DataFrame(rows)` with no `columns=` argument; the subsequent sort is gated `if not df.empty` (2537-2538), so when every candidate is gated out (empty `rows`) the function returns a 0-row, 0-column DataFrame straight through `_attach_drops_summary`. This directly contradicts the method's own docstring (lines 1344-1348: "Always carries th

## [LOW] engine_api.py:1036
**dte / delta query params are passed to the EV ranker unbounded; dte<=0 yields an internally inconsistent candidate (priced as 1-day, reported with raw/negative dte and a past expiration)**

- **Category:** input-validation  |  **Area:** api-surface
- **Failure scenario:** _handle_candidates coerces `dte_int = int(dte or 35)` with no sign/range check and forwards it as `dte_target` to rank_candidates_by_ev. The engine clamps only the pricing horizon: `T = max(dte_target, 1)/365` (wheel_runner.py:1456), so `?dte=0` or `?dte=-5` is priced as a 1-day option — but the API independently computes and returns `expiration_iso = _exp_base + timedelta(days=dte_int)` (line 1095), which for dte=0 is today and for dte=-5 is a date in the PAST, and echoes `"dte": dte_int` (line 1121) as 0/-5. The operator is shown a candidate whose reported expiration and DTE do not correspond to the contract the EV math actually priced (a 1-day option), and whose EV/prob_profit come from a degenerate ~0-length forward window. Same unvalidated passthrough exists on /api/tv/ranked, /api/tv/dossier, and /api/concentration_preview via their _parse_param(int) calls, none of which enforce dte>=1 or 0<delta<1.
- **Fix sketch:** Validate at the API boundary: reject dte<1 (and an unreasonable upper bound) and delta outside (0,1) with a 400 (BadParam), so the engine never receives a nonsensical horizon and the reported expiration/dte always match the priced contract.
- **Verifier:** Confirmed against the cited code. engine_api.py:1036 `dte_int = int(dte or 35)` inside a try/except that only defaults on TypeError/ValueError — non-numeric strings fall back to 35, but `int('0')`=0 and `int('-5')`=-5 parse without error, so no sign/range validation exists. dte_int is forwarded as dte_target (line 1050) to rank_candidates_by_ev. The engine clamps ONLY the pricing horizon at wheel_

## [LOW] engine_api.py:203
**Lazy singleton get_runner/get_connector have an unlocked check-then-set race under ThreadingHTTPServer**

- **Category:** concurrency  |  **Area:** api-surface
- **Failure scenario:** get_runner() (203-207) and get_connector() (210-224) do `if _x is None: _x = Construct()` with no lock. The server is a ThreadingHTTPServer (one thread per request) and both are lazily initialized on first use, so a cold-start burst (the dashboard fires 5-10 parallel fetches on hard reload, per the file's own S20 notes) can have two threads both observe `_runner is None` and each construct a WheelRunner, the second overwriting the module global. Requests already in flight keep a reference to the first instance while new requests get the second, so two connector/runner instances (and two data loads) coexist briefly. Construction is idempotent so this is benign in practice (wasted load, transient dual state), but it is the one genuinely non-atomic check-then-set on shared module state left unlocked — the money-critical nonce cache is correctly guarded by _TV_SEEN_NONCES_LOCK, and the ring buffers rely on append/pop being GIL-atomic.
- **Fix sketch:** Guard the lazy init with a module-level threading.Lock (double-checked locking), or construct both eagerly in main() before serve_forever().
- **Verifier:** Verified in engine_api.py. Module globals _runner/_connector are initialized to None (lines 107-108). get_runner (203-207) and get_connector (210-224) perform an unlocked check-then-set with no threading.Lock. The server is _EngineHTTPServer subclassing ThreadingHTTPServer (line 3323, instantiated line 3404) with daemon_threads=True (3405) — genuinely one thread per request. main() (3390-3447) doe

## [LOW] `INV` engine_api.py:2974
**Webhook 'chart agrees' gate is spoofable via getattr on the attacker-controlled alert.signal string**

- **Category:** downgrade-bypass  |  **Area:** advisors-bridge
- **Failure scenario:** _enrich_alert computes `agrees = sig.signal_action == alert.signal or getattr(sig, alert.signal.replace(' ', '_'), False)`. alert.signal comes straight from the (unauthenticated-by-default) TV webhook JSON. getattr against arbitrary attribute names on TVSignal means any truthy field name forces agrees=True: signal='ok' -> sig.ok is True; signal='close' -> the close price (truthy); signal='confidence' -> a positive float. With agrees forced True, a positive-EV, prob_profit>=0.65 candidate that should land in 'review' (verdict_reason='chart_disagrees') is upgraded to 'proceed' (verdict_reason='ev_above_threshold_and_chart_agrees'), defeating the Pine-chart-agreement downgrade and the intended human review. It cannot rescue a negative/zero EV (those are blocked/skip earlier), so the hard invariant holds, but the chart-agreement downgrade gate is trivially bypassed and a legitimately-mismatched signal name can also accidentally alias a truthy attribute.
- **Fix sketch:** Validate alert.signal against an explicit allowlist of known zone/signal strings before the getattr, or replace the getattr with an explicit dict lookup {'wheel_put_zone': sig.wheel_put_zone, 'covered_call_zone': sig.covered_call_zone, 'strangle_zone': sig.strangle_zone}.get(alert.signal, False). Never getattr on an untrusted string.
- **Verifier:** Confirmed the bug is real and reachable via the actual call path.

1. Untrusted source, no allowlist. `alert.signal` originates from webhook/query JSON: `TVAlert.parse` (engine/tv_signals.py:558) sets `signal=str(payload.get("signal",""))` with no allowlist; `is_valid()` (line 573) only checks non-empty. The GET `/api/tv/enrich` path (engine_api.py:2590) also builds `TVAlert(signal=signal_name)` s

## [LOW] `INV` engine_api.py:3164
**Webhook/enrich soft-warn ladder omits R10 single-name cap that the dossier reviewer applies**

- **Category:** downgrade-inconsistency  |  **Area:** advisors-bridge
- **Failure scenario:** The dossier path (EnginePhaseReviewer.review) downgrades proceed->review on a single-name concentration breach via R10 (check_single_name_cap, default 10% NAV), sitting beneath the 25% sector cap. The webhook/pull-enrich path in _enrich_alert reimplements the soft-warns inline (R7 VaR, R8a stress, R8b dealer regime, R9 sector cap) but never calls check_single_name_cap (grep confirms it is absent from engine_api.py). So a candidate that would push a single underlying over 10% NAV while staying under the 25% sector cap gets 'proceed' on /api/tv/enrich but 'review' on /api/tv/dossier for the same portfolio state — an inconsistent, over-optimistic verdict on the enrich surface. Money impact is bounded because the tracker still applies the hard single-name refusal at open_short_put time, but the advisory verdict shown to the operator is wrong.
- **Fix sketch:** Add a check_single_name_cap block in _enrich_alert mirroring the R9 sector-cap block (same proposed_notional, nav_val>0 guard), setting verdict='review'/verdict_reason='single_name_breach' when it fails, so the enrich ladder matches the dossier reviewer R7-R10.
- **Verifier:** Confirmed real and reachable. The enrich/webhook soft-warn ladder in engine_api.py::_enrich_alert (lines 3084-3180) applies R7 VaR (check_var, 3122), R8a stress (check_stress_scenario, 3139), R8b dealer regime (check_dealer_regime, 3153), and R9 sector cap (check_sector_cap, 3164) but never calls check_single_name_cap. A repo-wide grep over engine_api.py returns only check_sector_cap/sector_cap_br

