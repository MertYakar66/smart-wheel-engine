> _Point-in-time analysis report — generated by Claude Code on 2026-05-17. A snapshot of the repository and local data state at the commit noted below; not auto-updated. Saved to `docs/` at the user's request from the originating session._

# smart-wheel-engine data inventory — 2026-05-17

**Inventoried at:** `main @ 64b4f97` — uncommitted changes: **`theta.txt`** (untracked; a Theta-pull handoff note, not data — `git status --short` → `?? theta.txt`).

**Verifier scoping:** the **Git?** column is `Y` (in `git ls-files`), `N` (gitignored/untracked, local-only on this Windows laptop), or `AAPL` (only the AAPL shard is tracked). A downstream verifier can independently confirm only `Y` and `AAPL` rows.

**One-paragraph framing.** The live decision layer reads market data through exactly one surface: `engine/data_connector.py` (`MarketDataConnector`, 9 named CSVs in `data/bloomberg/`) plus `engine/data_integration.py` (earnings/dividends) — and, when `SWE_DATA_PROVIDER=theta` and the Terminal is up, live Theta calls. Grep confirms **no `engine/*.py` module reads `data_processed/theta/**`, `data/features/**`, or the `_yf` CSVs**. So `data_processed/theta/**` is a point-in-time *research/QA archive*, not a live input; the feature store and the `_yf` refresh files feed the `data/` feature pipeline (`data/consolidated_loader.py`), not the EV connector. This scoping drives the severities in Q2.

## Q1 — Verified data on hand

| Data type | Path | Git? | Provider | Universe | Date range (in-data) | Schema OK? | Freshness (mtime / data date) | Files / size | Notes |
|---|---|---|---|---|---|---|---|---|---|
| S&P 500 constituents (universe anchor) | `data_raw/sp500_constituents_current.csv` | Y | external | 503 names | snapshot (Date-added 1957→) | n/a — scripts only | 2026-03-25 / current snapshot | 1 / 54 KB | Denominator for all coverage ratios |
| Historical index membership (survivorship) | `data/bloomberg/sp500_index_membership.csv` | Y | bloomberg | 781 historical codes | 2015-01-01 .. 2026-01-01 | n/a — no `engine/` consumer | 2026-03-25 / 2026-01-01 | 1 / 982 KB | Feeds feature pipeline |
| Equity OHLCV (daily) | `data/bloomberg/sp500_ohlcv.csv` | Y | bloomberg | **503 / 503** | 2018-01-02 .. **2026-03-20** | ✓ `MarketDataConnector.get_ohlcv` | 2026-03-25 / 2026-03-20 | 1 / 60.8 MB | **Live EV input.** Col-rotation rename is load-bearing (connector L189) |
| Volatility / IV history | `data/bloomberg/sp500_vol_iv_full.csv` | Y | bloomberg | **503 / 503** | 2015-01-02 .. **2026-03-20** | ✓ `get_iv_history` | 2026-03-25 / 2026-03-20 | 1 / 81.2 MB | **Live EV input.** put/call IV + RV 30/60/90/260d |
| Liquidity (avg vol / turnover / shares) | `data/bloomberg/sp500_liquidity.csv` | Y | bloomberg | **503 / 503** | 2015-01-02 .. **2026-03-20** | ✓ `get_liquidity` | 2026-03-25 / 2026-03-20 | 1 / 69.0 MB | **Live EV input** |
| Vol + dividend + turnover panel | `data/bloomberg/sp500_vol_dvd.csv` | Y | bloomberg | 503 | 2018-01-02 .. 2026-03-20 | n/a — no `engine/` consumer | 2026-03-25 / 2026-03-20 | 1 / 42.6 MB | Feeds feature pipeline |
| Dividends (declared/ex/record/payable) | `data/bloomberg/sp500_dividends.csv` | Y | bloomberg | 427 / 503 | ex_date 1962-05-31 .. 2027-03-12 | ✓ `get_dividends` + `data_integration` | 2026-03-25 / future to 2027-03 | 1 / 3.97 MB | **Live EV input.** 76 names absent = non-payers (expected) |
| Earnings calendar (Bloomberg) | `data/bloomberg/sp500_earnings.csv` | Y | bloomberg | 503 / 503 | 1980-01-31 .. **2028-01-19** | ✓ `get_earnings`/`get_next_earnings` | 2026-03-25 / future to 2028-01 | 1 / 2.62 MB | **Live EV input** to event gate (has future scheduled dates) |
| Earnings calendar (yfinance refresh) | `data/bloomberg/sp500_earnings_yf.csv` | Y | yfinance | 498 / 503 | 2008-01-17 .. 2026-08-24 | ✓ schema parallels above | **2026-05-15** / future to 2026-08 | 1 / 587 KB | **Not on live path** — consumed by `data/consolidated_loader.py` only |
| Fundamentals snapshot (Bloomberg) | `data/bloomberg/sp500_fundamentals.csv` | Y | bloomberg | 503 / 503 | snapshot | ✓ `get_fundamentals`/`screen_universe` | 2026-03-25 | 1 / 109 KB | **Live EV input** |
| Fundamentals snapshot (yfinance) | `data/bloomberg/sp500_fundamentals_yf.csv` | Y | yfinance | 503 / 503 | snapshot | ✓ schema parallels above | **2026-05-15** | 1 / 73 KB | **Not on live path** — feature pipeline only |
| Historical (quarterly) fundamentals | `data/bloomberg/sp500_historical_fundamentals.csv` | Y | bloomberg | 503 | 2015-01-02 .. 2026-02-28 | n/a — no `engine/` consumer | 2026-03-25 / 2026-02-28 | 1 / 1.36 MB | Feeds feature pipeline |
| Credit risk (Altman-Z, coverage, S&P rating) | `data/bloomberg/sp500_credit_risk.csv` | Y | bloomberg | 500 / 503 | snapshot | ✓ `get_credit_risk` | 2026-03-25 | 1 / 21 KB | **Live EV input** (3 names absent) |
| Analyst estimates / ratings | `data/bloomberg/sp500_analyst.csv` | Y | bloomberg | 503 | snapshot | n/a — no `engine/` consumer | 2026-03-25 | 1 / 34 KB | best_eps/sales/target, rec count |
| Institutional ownership / float | `data/bloomberg/sp500_institutional.csv` | Y | bloomberg | 503 | snapshot | n/a — no `engine/` consumer | 2026-03-25 | 1 / 24 KB | |
| IV snapshot (today) | `data/bloomberg/sp500_iv_snapshot_today.csv` | Y | bloomberg | 503 | snapshot | n/a — no `engine/` consumer | 2026-03-25 | 1 / 25 KB | 30/60d ATM IV + RV30 |
| Treasury yield curve | `data/bloomberg/treasury_yields.csv` | Y | yfinance (refreshed) | n/a (4 tenors) | 2021-05-07 .. **2026-05-05** | ✓ `get_risk_free_rate` | **2026-05-15** / 2026-05-05 | 1 / 106 KB | **Live EV input.** Only series kept current (12 d old) |
| VIX term structure | `data/bloomberg/vix_term_structure.csv` | Y | bloomberg | n/a (vix/3m/6m) | 2018-01-02 .. **2026-03-20** | ✓ `get_vix`/`get_vix_regime` | 2026-03-25 / 2026-03-20 | 1 / 62 KB | **Live EV input** |
| VIX-full panel (vix/3m/6m/vvix/vx1/vx2) | `data/bloomberg/sp500_vix_full.csv` | Y | bloomberg | 6 instruments | 2015-01-02 .. 2026-03-20 | n/a — no `engine/` consumer | 2026-03-25 / 2026-03-20 | 1 / 395 KB | Includes **vx1/vx2 VIX futures** (front 2 months) |
| Macro panel (dxy/gold/spx/us_2y/us_10y/wti) | `data/bloomberg/sp500_macro.csv` | Y | bloomberg | 6 instruments | 2015-01-01 .. 2026-03-20 | n/a — no `engine/` consumer | 2026-03-25 / 2026-03-20 | 1 / 798 KB | Feeds feature pipeline |
| Sector-ETF OHLCV (11 SPDRs) | `data/bloomberg/sp500_sector_etfs.csv` | Y | bloomberg | 11 ETFs (XLB…XLY) | 2015-01-02 .. 2026-03-20 | n/a — no `engine/` consumer | 2026-03-25 / 2026-03-20 | 1 / 1.57 MB | |
| Vol-index family (long + wide) | `data_processed/vol_indices.parquet` + `_wide.parquet` | N | **yahoo** (`source` col) | 10 indices | 2021-04-26 .. **2026-04-23** | ✓ puller schema | ~2026-04-23 / 2026-04-23 | 2 / 339 KB | VIX9D/3M/6M, VVIX, VXN, SKEW, MOVE, OVX, GVZ |
| Option chains — live greeks snapshot | `data_processed/theta/chains/*.parquet` | N | **theta** | **495 / 503** (98.4%) | snapshot **2026-04-23**, ~35-DTE expiry | ✓ matches `ThetaConnector.get_option_chain` | ~2026-04-23 | 495 / 11 MB | Archive only — connector pulls Terminal live, not these files |
| IV surface — multi-expiry snapshot | `data_processed/theta/iv_surface/*.parquet` | N | **theta** | **28 / 503 (5.6%)** | snapshot 2026-04-23, expiries 2026-05→2027-01 | ✓ matches `get_iv_surface` | ~2026-04-23 | 28 / 1.1 MB | Mega-caps + 8 sector/index ETFs; SVI consumer dormant (D9) |
| Index option chains | `data_processed/theta/index_options_chains/*.parquet` | N | theta | 6 (SPX,SPXW,NDX,RUT,DJX,XSP) | snapshot 2026-04-23 | ✓ (NDX is quote-only — see note) | ~2026-04-23 | 6 / 256 KB | NDX file lacks greeks/iv (schema asymmetry) |
| Index option surfaces | `data_processed/theta/index_options_surfaces/*.parquet` | N | theta | 6 | snapshot 2026-04-23, expiries to 2027-03 | ✓ matches `get_iv_surface` | ~2026-04-23 | 6 / 456 KB | |
| ATM-IV history extract | `data_processed/theta/iv_history/*.parquet` | N | **bloomberg** (`source` col = `bloomberg`) | 493 / 503 | 2015-01-02 .. 2026-03-20 | ✓ (iv_atm/ticker/source) | ~2026-04-23 / 2026-03-20 | 493 / 25 MB | **Theta-named dir, Bloomberg-sourced data** |
| Stock EOD cache | `data_processed/theta/stocks_eod/*.parquet` | N | theta (window) — **ends at Bloomberg cutoff** | 493 / 503 | 2024-04-23 .. **2026-03-20** | ✓ (OHLCV+ticker) | ~2026-04-23 / 2026-03-20 | 493 / 12 MB | Provenance ambiguous (no `source` col) — see Open Qs |
| VIX-family history (legacy) | `data_processed/theta/vix_family/vix_family.parquet` | N | **cboe** (`source` col) | 4 (VIX/9D/3M/6M) | 2023-04-24 .. 2026-04-22 | ✓ puller schema | ~2026-04-23 / 2026-04-22 | 1 / 52 KB | Superseded by `vol_indices.parquet` |
| Feature store (8 groups, ticker-sharded) | `data/features/<group>/ticker=<X>/{data.parquet,metadata.json,stats.json}` | **AAPL** (26 files) | derived (`backfill_features.py`) | **494 / 503** per group | 2015 or 2018 → **2026-03-20** | ✓ verified AAPL+MSFT shards non-empty | git=commit / 2026-03-20 | 8×494 dirs / ~1.2 GB | Groups: dynamics, events, labels, options_features, regime, technical, vol_edge, volatility |
| Labeled trade dataset (ML) | `data_processed/trade_universe/2025-11-22_trade_universe.csv` | Y | derived | per-contract rows | snapshot 2025-11-22 | ✓ has future_pnl/win_flag/assignment_flag | 2026-03-25 / 2025-11-22 | 1 / 166 KB | Training labels; `future_pnl`/`win_flag` blank in sampled rows |
| Raw yfinance OHLCV (sample fixture) | `data_raw/ohlcv/*.csv` | Y | yfinance | 5 (ABBV,ABT,ACN,AOS,MMM) | → 2025-11-21 | ✓ (malformed 2nd header row — see Q2-G) | 2026-03-25 / 2025-11-21 | 5 / ~2 MB | Test/sample fixtures only |
| Raw yfinance option chains (sample) | `data_raw/yfinance/options/2025-11-22_*.csv` | Y | yfinance | 5 | snapshot 2025-11-22 | ✓ | 2026-03-25 / 2025-11-22 | 5 | Test/sample fixtures only |
| Theta capability/tier map | `data_processed/theta_capabilities.json` | N | derived (probe) | n/a | probed 2026-04-23 | ✓ | ~2026-04-23 | 1 / 9.6 KB | Tier truth — see Q2/Q3 |
| Theta pull manifest | `data_processed/theta/_manifest.json` | N | derived | n/a | last run **2026-04-23T04:53:50Z** | ✓ | ~2026-04-23 | 1 | 5 runs logged; no `_manifest_failures_*.json` sidecars on disk |

**Present but unverified** (path exists, fails the non-empty / schema standard):
- `data/bloomberg/sp500_corporate_actions.csv` — **2 bytes, empty** (1 line, no header). Intended target of `pull_theta_corp_actions.py`. **No `engine/` consumer** (grep clean) → emptiness has no decision-layer impact.
- `data/bloomberg/sp500_iv_history.csv` — **20 bytes, header only** (`date,ticker,iv_30d`, 0 rows). **No `engine/` consumer** — the connector's IV path uses `sp500_vol_iv_full.csv`.
- `data/bloomberg/sp500_short_interest.csv.xlsx` — 62 KB, **`.xlsx` not `.csv`**; not in `MarketDataConnector._FILES`; not loadable by the CSV connector as-named.

## Q2 — Missing or partial

**No BLOCKERs.** All 9 connector CSVs are present with schemas matching `engine/data_connector.py`; the decision layer (`ev_engine`, `wheel_runner`, `candidate_dossier`, `option_pricer`) can run for the full 503-name universe.

### A. Core Bloomberg time-series are 58 days stale — **HIGH**
- **Evidence:** `sp500_ohlcv.csv`, `sp500_vol_iv_full.csv`, `sp500_liquidity.csv`, `sp500_vol_dvd.csv`, `vix_term_structure.csv`, `sp500_macro.csv`, `sp500_vix_full.csv`, `sp500_sector_etfs.csv` all end **2026-03-20** (today 2026-05-17 → **58 days**).
- **Impact:** these are the live EV inputs read on *both* providers for the history-window quant modules — `forward_distribution.py` (block bootstrap + HAR-RV), `tail_risk.py` (POT-GPD), `regime_hmm.py`, `portfolio_copula.py`, `realized_vol.py`. Every forward distribution / regime / tail / CVaR estimate is computed on a window ending ~2 months ago.
- **Note:** `treasury_yields.csv` (ends 2026-05-05, 12 d), `sp500_earnings_yf.csv`/`sp500_fundamentals_yf.csv` (mtime 2026-05-15) were refreshed via yfinance; the rest were not. `pull_ohlcv.py` requires a Bloomberg Terminal (`xbbg`).

### B. News-sentiment data absent — **MEDIUM**
- **Evidence:** `engine/news_sentiment.py:45-47` probes `data_processed/news_sentiment.parquet`, `data/news/sentiment.parquet`, `data_processed/news_sentiment.csv`; none exist. `pull_news_sentiment.py` docstring confirms `sentiment_multiplier(ticker)` returns 1.0 for every candidate.
- **Impact:** the news-sentiment reviewer on the EV path is inert (neutral 1.0 multiplier for all 503 names).

### C. `iv_surface/` snapshot coverage 28 / 503 (5.6 %) — **LOW**
- **Evidence:** 28 parquet files (AAPL, AMZN, BAC, COP, CVX, DIA, GOOGL, GS, IWM, JNJ, JPM, LLY, META, MSFT, MS, NFLX, NVDA, PFE, QQQ, SPY, TSLA, UNH, WFC, XLE, XLF, XLK, XLV, XOM).
- **Impact:** the SVI consumer `engine/volatility_surface.py` is **dormant** per **D9** (zero non-test callers). No live decision-path impact today.

### D. Theta archive is the 2026-04-23 snapshot — PROJECT_STATE's 2026-05-04 pull is absent — **LOW (engine) / discrepancy**
- **Evidence:** `data_processed/theta/` contains only `chains, index_options_chains, index_options_surfaces, iv_history, iv_surface, stocks_eod, vix_family` + `_manifest.json`. `PROJECT_STATE.md §3.4` claims a 2026-05-04/05 pull produced **`iv_surface_history/` (381)** and **`options_flow/` (499)** — *neither directory exists on disk.* `data_processed/theta/` dir mtime = `Apr 23 01:40`; `_manifest.json` last run = `2026-04-23T04:53:50Z`.
- **Impact:** none on the engine — grep confirms no `engine/*.py` references `iv_surface_history`, `options_flow`, `option_tape`, or `corporate_actions`. Flagged because PROJECT_STATE asserts data that is not here. → Open Question.

### E. Theta per-ticker coverage 493-495 / 503 — **LOW**
- **Evidence:** `chains` 495 (missing 8), `iv_history`/`stocks_eod` 493 (missing 10). Missing set: `BF-B, BRK-B, DAY, HOLX, IPG, K, MMC, NVR` (+ `CBOE, LKQ, MHK, SOLS`). `BF-B`/`BRK-B` are partly a hyphen-vs-dot filename artifact; `DAY, NVR, BF.B, BRK.B` are the documented persistent-failure set (HTTP 472).
- **Impact:** ≤2 % gap on an archive the engine doesn't auto-load. Minimal.

### F. Provenance mislabeling inside `data_processed/theta/` — **LOW (data-integrity note)**
- **Evidence:** `source` column: `iv_history/*.parquet` → `bloomberg` (2820/2820 rows); `vix_family.parquet` → `cboe`; `vol_indices.parquet` → `yahoo`. `stocks_eod` has no `source` column and ends at the exact Bloomberg cutoff 2026-03-20.
- **Impact:** the directory name `theta/` implies Theta provenance; only `chains/`, `iv_surface/`, `index_options_*` are genuinely Theta-sourced. No engine impact (not auto-loaded), but misleading.

### G. Schema drift — doc/code & file-format — **LOW**
- **`_yf` merge claim:** `pull_fundamentals_yf.py` / `pull_earnings_yf.py` docstrings and `PROJECT_STATE.md §3` state the `_yf` files are "merged by the wheel runner." Grep: `consolidated_loader|fundamentals_yf|earnings_yf` matches **no `engine/` file**. The live EV connector reads the Bloomberg originals.
- **Puller path mismatch:** `pull_ohlcv.py` writes `data/bloomberg/ohlcv/sp500_ohlcv.csv` (subdir); the connector reads `data/bloomberg/sp500_ohlcv.csv`. *(Fixed subsequently — PR #87.)*
- **`data_raw/ohlcv/*.csv`:** carry a malformed 2nd header row (`,ABBV,ABBV,…`) — a yfinance multi-index export artifact. 5-ticker sample fixtures only.

### H. Provider asymmetry (relevant to `SWE_DATA_PROVIDER` + D11) — informational
- `bloomberg` provider has **no option chain** (`get_option_chain` is `ThetaConnector`-only) → in sandbox/CI, `dealer_positioning.py` `MarketStructure` is simply absent (optional kwarg). `theta` provider has no fundamentals/corporate-actions (inherits Bloomberg CSV). Per **D11**, per-endpoint Theta failures must raise `PerEndpointFailure` (no silent CSV substitution).

## Q3 — Wishlist

1. **Fresh OHLCV + IV history past 2026-03-20** — Strengthens `forward_distribution.py`, `tail_risk.py`, `regime_hmm.py`, `portfolio_copula.py`, the feature store. Fixes: every forward distribution, HMM regime, POT-GPD tail and copula-CVaR estimate is fit on a window ending 2026-03-20 (Q2-A). Provider: theta (`stock/history/eod` OK) / bloomberg / yfinance. Effort: existing puller idle.
2. **Extend OHLCV history back to 2015 (currently 2018-01-02)** — Strengthens `tail_risk.py` (POT-GPD), `forward_distribution.py` (HAR-RV). Fixes: ~2,065 daily returns/ticker yields only ~21-103 exceedances at a 95-99% threshold — at/below the ~100-exceedance rule-of-thumb for a stable GPD shape. Provider: bloomberg/theta/yfinance. Effort: existing puller, wider start date.
3. **Recent/rolling option-chain coverage beyond the single 2026-04-23 snapshot** — Strengthens `dealer_positioning.py`, `ev_engine.py` cost model. Fixes: dealer GEX/walls and chain-based slippage run on a 24-day-old book or are absent (bloomberg mode). Provider: theta. Effort: existing puller idle.
4. **News-sentiment feed** — Strengthens `engine/news_sentiment.py`. Fixes: `sentiment_multiplier()` returns 1.0 for all 503 names (Q2-B). Provider: external (Polygon/Finnhub free tier). Effort: existing puller idle — needs API key.
5. **Options-flow aggregates (put/call volume ratio, OI deltas, unusual-volume flag)** — Strengthens `dealer_positioning.py`. Fixes: dealer-flow classification uses `open_interest` only. Provider: theta endpoint `option/history/volume` is **MISSING (404)** at the current tier — needs tier upgrade or external vendor. Effort: puller exists but endpoint tier-blocked.
6. **VIX futures term-structure curve (UX1-UX8)** — Strengthens `regime_detector.py`. Fixes: the front-month-VX-minus-spot contango signal has no live source. Provider: **neither** readily — all Theta `future/*` endpoints MISSING (404); Bloomberg BQL gap. Effort: external vendor (CBOE free EOD VX settlement).
7. **Full-universe IV-surface expansion (28 → 503)** *(SVI / `iv_surface/`, dormant per D9)* — **Missing-data contract (required by D9): loud-fail.** `get_iv_surface()` already returns an empty DataFrame on missing data; the wiring should raise/skip uncovered tickers as an explicit, logged skip — never a silent flat-IV stub. Strengthens `volatility_surface.py`, `skew_dynamics.py`. Provider: theta. Effort: existing puller idle.

## Open questions for the human

1. **Is the 2026-05-04/05 Theta pull expected on this clone?** `PROJECT_STATE.md §3.4` describes `iv_surface_history/` (381) and `options_flow/` (499) as landed on disk. Neither exists here; `data_processed/theta/` is untouched since 2026-04-23.
2. **Provenance of `data_processed/theta/stocks_eod/`** — no `source` column, ends at the exact Bloomberg cutoff (2026-03-20), but start (2024-04-23) matches the Theta connector's 2-year default window. Theta stock EOD or a Bloomberg fallback?
3. **Is the `_yf` → live-path merge intended?** Puller docstrings say the wheel runner merges the `_yf` files; no `engine/` code does.
4. **`treasury_yields.csv` starts 2021-05-07** (~5 y) where other Bloomberg series reach 2015/2018. Intentional, or a truncation?
5. Should the empty `sp500_corporate_actions.csv` / `sp500_iv_history.csv` and the misnamed `sp500_short_interest.csv.xlsx` remain tracked, or be untracked until populated?

## Methodology log

**Branch pin:** `git rev-parse --abbrev-ref HEAD` → `main`; `--short HEAD` → `64b4f97`; `git status --short` → `?? theta.txt`.

**Read in order:** `CLAUDE.md`, `AGENTS.md`, `PROJECT_STATE.md`, `MODULE_INDEX.md`, `TESTING.md`, `DECISIONS.md` (D9 + D11), `engine/data_connector.py` (full), `engine/theta_connector.py` (full), `DATA_POLICY.md`; module docstrings + first ~50 lines of all 17 `scripts/pull_*.py`.

**Enumeration (sampled, never blind-recursed):** `find … -maxdepth 2/3 -type d`; `du -sh` per directory; `git ls-files <path>` for tracked/untracked. Parquet read with Python 3.12.10 + pyarrow 23.0.0. No pullers run, no Theta/Bloomberg API hit, no file modified.

**Evidence for Q1 numeric claims** — `wc -l data_raw/sp500_constituents_current.csv` → `504` (= 503 + header). `pd.read_csv(usecols=[date,ticker])` per Bloomberg CSV: `sp500_ohlcv` rows=988,809 ids=503 `2018-01-02..2026-03-20`; `sp500_vol_iv_full` 1,361,615 / 503 / `2015-01-02..2026-03-20`; `sp500_liquidity` 1,362,737 / 503; `sp500_vol_dvd` 988,837 / 503; `sp500_dividends` 50,230 / 427 / `1962-05-31..2027-03-12`; `sp500_earnings` 49,379 / 503 / `1980-01-31..2028-01-19`; `sp500_earnings_yf` 12,242 / 498 / `..2026-08-24`; `sp500_historical_fundamentals` 30,347 / 503 / `..2026-02-28`; `treasury_yields` 1,254 / `2021-05-07..2026-05-05`; `vix_term_structure` 2,094 / `2018-01-02..2026-03-20`; snapshots `sp500_fundamentals` 503, `sp500_credit_risk` 500, `sp500_analyst` 503, `sp500_institutional` 503, `sp500_iv_snapshot_today` 503. Empty: `sp500_corporate_actions.csv` (2 bytes), `sp500_iv_history.csv` (20 bytes). Theta subdir counts via `find -maxdepth 1 -type f | wc -l`: chains 495, iv_history 493, stocks_eod 493, iv_surface 28, index_options_chains 6, index_options_surfaces 6, vix_family 1. Feature store: 494 `ticker=*` dirs per group × 8; `git ls-files data/features` → 26 (AAPL only). `_manifest.json` last run `2026-04-23T04:53:50Z`; `theta_capabilities.json` probed 2026-04-23 (`option/history/volume` MISSING 404, all `future/*` MISSING 404, `index/snapshot` BLOCKED 403). `source`-column provenance: `iv_history`→`bloomberg`, `vix_family`→`cboe`, `vol_indices`→`yahoo`.

**Skipped deliberately:** `scripts/diagnose_candidates.py` full-universe (~3 min); reading `.env`; any puller execution.
