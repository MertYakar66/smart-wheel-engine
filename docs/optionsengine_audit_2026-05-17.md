> _Point-in-time analysis report — generated by Claude Code on 2026-05-17 (read-only audit, repo at `main @ 981c5d2`). A snapshot, not auto-updated. Saved to `docs/` at the user's request from the originating session._

# OptionsEngine.txt — scenario-driven accuracy audit

**Method (read-only):** `main @ 981c5d2`; `SWE_DATA_PROVIDER=bloomberg`; installed `arch 8.0.0` + `hmmlearn 0.3.3` to enable execution; booted `engine_api.py` and hit 14 endpoints; ran 5 doc snippets; introspected every named module/symbol via Python `inspect`; `pytest --co` for the test count. **The doc's illustrative numbers/JSON were never reproduced as real output** — every figure below is either real captured output or marked CANT-RUN.

**Verdict legend:** PASS = accurate · STALE = was-true / wrong detail / outdated number · MISSING = named thing doesn't exist · CANT-RUN = not executable here (static-verified where possible).

## The 3 questions resolved

| # | Question | Resolution |
|---|---|---|
| 1 | "1051 automated tests" — real count? | **STALE.** `pytest tests/ --co -q` → **`1734 tests collected`**. Doc says 1051 three times (§8, §10×2) — off by 683. |
| 2 | `src/features/assignment.py` + `dynamics.py` — live or phantom? | **Phantom — not on any live path.** Both import fine; `AssignmentFeatures.roll_vs_assignment_score(...)` has exactly the doc's signature. But repo-wide grep: the only non-test importers are `data/feature_pipeline.py` (offline feature-store builder) and `src/features/__init__.py`. **No `engine/` module and not `engine_api.py` import them** (`engine_api.py:1089` imports `src.features.technical` only). Per D2 they are deprecated-phantom: callable library code, no EV-path wiring, no endpoint. Example 4's "the engine wakes the trader" framing is overstated. |
| 3 | JSON shapes (`dealer_positioning`, `tv/dossier`) match the code? | **`tv/dossier`: PASS** — real `{"dossiers":[{ticker,ev_row,chart_context,verdict,verdict_reason,review_notes,has_chart,built_at}]}`; every doc-claimed key present, real adds `has_chart`/`built_at` + richer `ev_row`/`chart_context`. **`dealer_positioning`: PASS (static).** Endpoint returns HTTP 404 `{"error":"option chain unavailable"}` in bloomberg mode — CANT-RUN by execution. `DealerPositioningAnalyzer.analyze()` returns a `MarketStructure` dataclass; **all 15 keys** in the doc's §2.5 JSON exist as `MarketStructure` fields. |

## §1 — Mental model / four layers

| Claim | Verdict | Evidence |
|---|---|---|
| 25 named `engine/` + `data/` modules exist | **PASS** | All 25 import OK under `importlib` (incl. `binomial_tree`, `monte_carlo`) |
| `data/pipeline.py`, `financial_news/`, `news_pipeline/` exist | **PASS** | trees + files confirmed on disk |
| EV invariant: no tradeable candidate bypasses `EVEngine.evaluate`; reviewers downgrade-only | **PASS** | CLAUDE.md §2 / DECISIONS D1; `test_audit_invariants.py` etc. |
| Interface: REST API, Next.js dashboard, TV bridge, CLI dashboard, local agent, committee | **PASS** | `engine_api.py` boots; `dashboard/`, `tradingview/`, `local_agent/`, `advisors/` exist |

## §2 — Pre-market ritual

| Claim | Verdict | Evidence |
|---|---|---|
| 2.1 `python engine_api.py` → HTTP server on :8787 | **PASS** | Booted; `/api/status` HTTP 200 |
| 2.2 `financial_news/processing/brief_generator.py`, `news_pipeline/security/classifier.py`, `story_clusterer` | **PASS (exist)** | all 3 files confirmed; behavior CANT-RUN |
| 2.3 `/api/regime`, `/api/vix` | **PASS** | HTTP 200 |
| 2.3 `NelsonSiegelTermStructure`, `regime_detector`, `regime_hmm` | **PASS** | classes/modules import; `hmm_multiplier`=0.556 observed in real rank output |
| 2.3 HMM `position_multiplier()` in `[0, 1.25]` | **CANT-RUN (range)** | module imports; exact bound not traced — plausible |
| 2.4 `/api/candidates`; `WheelRunner.rank_candidates_by_ev` | **PASS** | endpoint HTTP 200; ran 5-ticker rank → 5 rows × 40 cols |
| 2.4 History gate "≥504 trading days" | **PASS** | real signature `min_history_days: int = 504` |
| 2.4 Event gate / chain-quality gate kwargs | **PASS** | `use_event_gate`, `earnings_buffer_days=5`, `enforce_chain_quality_gate` in signature |
| 2.4 Forward-dist cascade; `distribution_source` = `empirical_non_overlapping` | **PASS** | `best_available_forward_distribution` exists; real output `distribution_source=['empirical_non_overlapping']` |
| 2.4 `prob_touch` is a candidate field | **STALE** | not in `rank_candidates_by_ev` columns |
| 2.4 "Every row carries `authority="ev_ranked"`" | **STALE** | `authority` is a **top-level** `/api/candidates` field (`= "ev_ranked"`), **not** per-row |
| 2.5 `/api/tv/dealer_positioning`; `DealerPositioningAnalyzer.analyze()` | **PASS (static) / CANT-RUN (exec)** | endpoint 404 in bloomberg mode (no chain); `analyze()` returns `MarketStructure` |
| 2.5 dealer JSON keys (gex/dex/vanna/charm/flip/walls/pinning) | **PASS** | all 15 doc keys ∈ `MarketStructure` fields |
| 2.5 `DealerAssumption.LONG_CALLS_SHORT_PUTS` vs `SHORT_BOTH` | **PASS** | enum members = exactly those two |
| 2.5 dealer multiplier clamped `[0.70, 1.05]` | **PASS** | CLAUDE.md §2 / DECISIONS D1 |

## §3 — Example 1 (AAPL wheel entry)

| Claim | Verdict | Evidence |
|---|---|---|
| `/api/analyze/<ticker>` → `TickerAnalysis` (fundamentals/vol/events/regime) | **PASS** | HTTP 200; keys incl. `marketCap,peRatio,beta,creditRating,iv30d,rv30d,ivRank,volRiskPremium,nextEarningsDate,stranglePhase` |
| `/api/calendar?ticker=&days=` from `event_calendar.py` | **PASS** | HTTP 200, `{events:[...]}` |
| `/api/committee`; `CommitteeEngine` + 4 personas | **PASS** | HTTP 200; `CommitteeEngine.evaluate()` exists; `advisors.{buffett,munger,simons,taleb}` import; response has `advisors,ev_anchored,authority` |
| `use_event_gate=False` research override | **PASS** | kwarg present in `rank_candidates_by_ev` signature |
| Committee is a gate, cannot upgrade negative EV | **PASS** | DECISIONS D1; `ev_anchored` field in committee response |

## §4 — Example 2 (MSFT TradingView dossier)

| Claim | Verdict | Evidence |
|---|---|---|
| `/api/tv/dossier`; `WheelRunner.build_candidate_dossiers` | **PASS** | endpoint HTTP 200; method exists |
| `EnginePhaseReviewer` rules R1–R6; thresholds | **PASS** | class exists; `min_proceed_ev=10.0`, `spot_tolerance_pct=0.02` (= R5 / R3 "2%") |
| Dossier JSON shape | **PASS** | real keys ⊇ doc keys |
| Doc example `verdict:"proceed"` | **STALE (illustrative)** | real run → `verdict:"review"`, `verdict_reason:"chart_context_missing"` (no screenshot) — confirms R2 |
| `FilesystemChartProvider`/`PlaywrightChartProvider`/`ChainedChartProvider` | **PASS** | all 3 classes import |
| `/api/memo`; `MemoGenerator` via Ollama | **CANT-RUN** | `MemoGenerator` exists; `/api/memo` did not return within 90s (Ollama dependency) |
| Margin "from `calculate_reg_t_margin_short_put`" | **PASS** | exists: `transaction_costs.calculate_reg_t_margin_short_put(strike,underlying_price,premium)` |
| Sizing "from `engine/risk_manager.py::position_size_kelly`" | **STALE/MISSING** | no `position_size_kelly`. Real: `risk_manager.calculate_kelly_fraction()` + `RiskManager.calculate_position_size()` |
| `wheel_tracker.py` NO_POSITION→SHORT_PUT logging | **PASS (module)** | `wheel_tracker` imports |
| No auto broker execution (ib_insync stub) | **PASS** | consistent with CLAUDE.md's NEVER list |

## §5 — Example 3 (SPX strangle)

| Claim | Verdict | Evidence |
|---|---|---|
| `/api/strangle`, `/api/chart/strangle`; `StrangleTimingEngine` / `StrangleTimingWithIV` | **PASS (exist) / partial** | both classes import; `/api/strangle?ticker=SPY` returned `{error,ticker}` — SPY isn't an S&P-500 constituent, not in the Bloomberg CSVs |
| `/api/expected_move`, `/api/payoff`; `payoff_engine.py` | **PASS** | both HTTP 200 |
| `portfolio_cvar_copula(...)` signature + keys | **PASS** | ran it — returns dict; doc's keys `gaussian_cvar,t_cvar,tail_amplification,verdict` all present |
| `portfolio_cvar_copula` numbers ($12,400 / $17,800 / 1.44×) | **STALE (illustrative)** | real run: `tail_amplification=1.10, verdict='mild_tail_dependence'` |
| `StressTester.greeks_stress_ladder(...)` + `reliable` col + `attrs["residual_gate_passed"]` + 10% tolerance | **PASS** | ran it — `reliable` bool col present; `attrs={residual_tolerance:0.1, residual_gate_passed:False,...}`; warning fired "residual exceeds 10% tolerance" |
| Stress ladder column `spot_shock` | **STALE** | real columns are `spot_change`/`spot_change_pct` — `ladder[["spot_shock",...]]` would `KeyError` |
| `tester.run_scenario("2008 Financial Crisis")` (bare string) | **STALE/wrong-usage** | real sig `run_scenario(scenario: Scenario, positions, spot_prices, portfolio_value)` |
| `risk_manager.py::calculate_monte_carlo_var`, Cholesky + jump diffusion | **PASS** | exists as `RiskManager.calculate_monte_carlo_var(...)` with `include_jump_diffusion/...` |

## §6 — Example 4 (rolling vs assignment)

| Claim | Verdict | Evidence |
|---|---|---|
| `/api/portfolio`; `WheelRunner.portfolio_report` | **PASS** | endpoint routed; method exists `-> dict` |
| Portfolio Greeks "from `aggregate_portfolio_greeks`" | **STALE/MISSING** | no `aggregate_portfolio_greeks`. Real: `RiskManager.calculate_portfolio_greeks()` |
| `from src.features.assignment import AssignmentFeatures` / `roll_vs_assignment_score(...)` | **PASS (signature) / STALE (framing)** | exact signature match — but src/ is D2-phantom, unwired (see Q2) |
| `wheel_tracker.py` state machine, transaction-cost logging | **PASS (module)** | `wheel_tracker`, `transaction_costs` import |
| `simulate_option_trade(...)` → `TradeOutcome` with gross P&L / costs / max profit-loss | **PASS** | ran it — `TradeOutcome` fields incl. `gross_pnl,entry_costs,exit_costs,assignment_costs,max_profit_reached,max_loss_reached` |
| Snippet `ohlcv_df=msft_history` | **STALE (shape)** | `simulate_option_trade` needs a **`Date`-column** df; `MarketDataConnector.get_ohlcv()` returns a `date`-**indexed** df — raw connector output `KeyError`s until reshaped |

## §7 — Example 5 (end-of-day review)

| Claim | Verdict | Evidence |
|---|---|---|
| `/api/candidates?as_of=` PIT filter | **PASS** | `as_of` param in `rank_candidates_by_ev` signature |
| `ml/wheel_model.py`; `DriftDetector.check_calibration(...)` | **PASS** | module imports; ran `check_calibration` — real keys `{brier_score,ece,passed,reliability,gate_reason}` **exactly match** the doc |
| `check_calibration` output `gate_reason:"ok"`, `passed:true` | **PASS (shape) / STALE (numbers)** | my run: `passed=False`, detailed `gate_reason` string — doc's is the passing-case example |
| `ml/model_governance.py::ChampionChallenger` | **PASS** | class exists (`promote_to_champion/shadow`, `evaluate_switch`, `register_model`) |
| `DataPipeline.audit_survivorship_bias(...)` | **PASS** | method exists with the doc's exact signature `-> dict` |
| snippet `pipeline = get_data_pipeline()` | **MISSING** | no `get_data_pipeline` anywhere (`grep "def get_data_pipeline"` → 0 hits) |
| snippet `load_historical_sp500_members(...)` | **MISSING** | no such function anywhere — Example 5 Action 3 snippet `NameError`s twice |
| `scripts/download_sp500_constituents.py` exists | **PASS** | confirmed |
| `local_agent/` Playwright agent, `chroma_manager.py` | **PASS (exist) / CANT-RUN** | `local_agent/memory/chroma_manager.py` exists; runtime CANT-RUN |

## §8 / §9 / §10 — capabilities, limits, summary

| Claim | Verdict | Evidence |
|---|---|---|
| "30+ REST API endpoints" | **PASS** | `engine_api.py` routes ~33 GET/POST endpoints |
| All 14 endpoints the walkthrough uses exist | **PASS** | confirmed in `do_GET` routing (lines 220–333) |
| BSM + 3rd-order Greeks; binomial CRR; LSM; Merton jump-diffusion | **PASS (modules)** | `option_pricer`, `binomial_tree`, `monte_carlo` import; MODULE_INDEX corroborates |
| POT-GPD CVaR; Gaussian HMM; Nelson-Siegel; t-copula; SVI; GEX/walls/flip | **PASS (modules)** | all named modules import; `cvar_99_evt`/`tail_xi` real columns in rank output |
| "**1051 automated tests**" (§8 + §10 ×2) | **STALE** | real = **1734** |
| "Zero regressions across 5 audit sprints" | **CANT-RUN** | not a structural claim — unverified |
| "locked in by test `test_candidates_does_not_call_screen_candidates`" | **PASS** | test exists in `tests/test_launch_blockers.py` |
| §9 limits (no auto-exec, no tick flow, no historical GEX, equities-only, no vision-LLM, charm/vanna observability-only) | **PASS (consistent)** | matches CLAUDE.md's NEVER list / DECISIONS; `src/features/dynamics.py` exists |

## Overall accuracy assessment

**The doc is structurally accurate and detail-stale.** Of ~120 concrete claims extracted: the **module / endpoint / class / invariant / gate** layer is ~95%+ correct — every one of the 25 modules imports, all 14 endpoints exist and (except Ollama/Theta-gated ones) return HTTP 200, and the hard numbers that *are* right are right for good reasons (`min_history_days=504`, `spot_tolerance_pct=0.02`, `heavy_tail_penalty=0.5`, `residual_tolerance=0.1`, dealer clamp `[0.70,1.05]`, `DealerAssumption` enum, the signatures of `portfolio_cvar_copula`/`check_calibration`/`simulate_option_trade`/`EnginePhaseReviewer`).

**The concentrated failures — a reader should not trust these:**
- **Stale number:** "1051 tests" (×3) → really **1734**.
- **Wrong function names:** `position_size_kelly`, `aggregate_portfolio_greeks` (capabilities exist as `calculate_kelly_fraction` / `RiskManager.calculate_position_size` / `RiskManager.calculate_portfolio_greeks` — note `calculate_position_size`/`calculate_portfolio_greeks` method names also appear on a class in `engine/contracts.py`; cite `engine/risk_manager.py` explicitly when correcting the doc).
- **Two functions that don't exist:** `get_data_pipeline()`, `load_historical_sp500_members()` — the Example 5 snippet would `NameError`.
- **Wrong usage/columns:** `run_scenario("2008 Financial Crisis")` (needs a `Scenario` object + 3 args); stress-ladder column `spot_shock` (real: `spot_change`).
- **Imprecise:** `authority="ev_ranked"` is one top-level field, not "every row"; `prob_touch` isn't a candidate column; `simulate_option_trade`'s `ohlcv_df` needs a `Date` column, not the connector's index-form output.
- **Overstated framing:** Example 4 presents `src.features.assignment` as a live engine component — it's an unwired D2-phantom library call.
- **Illustrative-as-real:** every quoted output (candidate table, dealer JSON, `$12,400`/`$17,800`/`1.44×`, `brier 0.168`) is example data — real runs produce different values; none were reproduced as real.

**Bottom line:** trustworthy as an architecture map (layers, modules, endpoints, invariants, gates); **not** trustworthy as a copy-paste API reference (≈4 broken snippets) or as a source of any specific number. The drift is uniformly *downward-stale* — the repo has more tests and a refactored `risk_manager` since the doc was written (mtime 2026-04-14). Recommend the doc not be used to "narrate scenarios as a user" until the items above are corrected.
